# 이 대화 전용 메모리 (세션 5)

이 파일은 이 대화(세션 5)에서만 사용합니다. 다른 대화의 `.teleclaude/memory.md`나 다른 번호의 메모 파일과 섞지 마세요.

## 2026-07-16: 상태 카드가 와이파이 신호를 놓치자마자 "자리비움"으로 바뀌는 문제 수정

- **사용자 지시**: "자리비움 처리가 이상하다. 와이파이 신호를 놓치자 마자 자리비움으로 가는게 아니다. 자리비움 설정시간만큼 지났을때 그 때 자리비움 기록이 되어야 한다."
- **원인 분석**: 실제 DB 이벤트 기록(`WifiMonitorService.checkWifiState()`)은 원래부터 정확했음 — 자리비움/퇴근 이벤트는 항상 자리비움 인정 기준(기본 10분)이 지난 뒤에만 기록되도록 이미 구현돼 있었음. 문제는 **상태 카드에 표시되는 실시간 라벨**(`MainActivity.kt`의 `commuteStatus()`)이었음 — `isAtWork && !detectedNow`이기만 하면(즉 와이파이가 안 잡히는 바로 그 순간부터) 유예 기간과 무관하게 즉시 "자리비움"으로 표시되고 있었음. 실제 기록은 맞는데 화면 라벨만 성급하게 바뀌는 표시 전용 버그.
- **수정**:
  - `CommuteViewModel.kt`에 `awaySinceAt: StateFlow<Long?>` 신설(`settingsRepository.awaySinceAt`를 그대로 노출 — 백엔드가 자리비움 유예를 재는 것과 동일한 실제 값을 그대로 씀, UI에서 별도로 다시 계산 안 함).
  - `MainActivity.kt`: `CommuteScreen`에서 `awaySinceAt`, `absenceThresholdMinutes`(이미 있었지만 안 쓰고 있었음)를 새로 수집. `commuteStatus()` 시그니처에 이 둘을 추가해서, `awaySinceAt`이 null이거나 `now - awaySinceAt < 임계값`이면(아직 유예 중이면) `isAtWork && !detectedNow`여도 "자리비움"이 아니라 그대로 "근무중"으로 표시. 임계값을 실제로 넘은 뒤에만 "자리비움"으로 전환. `CompactStatusCard`에도 두 파라미터 추가해서 그대로 전달.
  - 점심시간 특수 로직(점심시간 중엔 아무리 오래 끊겨도 퇴근 처리 안 함)은 이 계산에서 별도로 신경 안 씀 — 점심시간이어도 자리비움 인정 기준을 넘으면 "자리비움 · 점심시간"으로 표시되는 게 자연스럽다고 판단(기존에도 `isLunchTimeNow`가 별도 접미사로 붙는 구조라 자연히 맞물림).
- **실기기 검증**: 재빌드(`assembleDebug`)+재설치(`installDebug`) 후 앱 강제종료→재실행, 크래시 없음. 현재 회사 와이파이 연결 상태에서 상태 카드가 "근무중"으로 정상 표시되는 것 확인. (실제로 와이파이를 끊고 유예 기간 동안 "근무중"이 유지되다가 임계값을 넘는 순간 "자리비움"으로 바뀌는 것까지는 실기기를 물리적으로 이동시켜야 재현 가능해서 이번엔 코드 로직 검토로 갈음 — adb만으로는 재현 불가.)
- **How to apply**: 이 앱은 "실제 기록되는 이벤트"와 "화면에 보여주는 실시간 상태"가 서로 다른 계산 경로를 갖고 있어서, 데이터 기록 로직이 맞다고 해서 화면 표시 로직도 맞다는 보장이 없음 — 사용자가 "이상하다"고 느끼는 지점이 항상 DB 로직이 아니라 표시 로직(`commuteStatus`, `MainActivity.kt`)일 수 있다는 걸 염두에 둘 것. 다음에 비슷한 "화면에 보이는 상태가 이상하다"는 리포트가 오면 `WifiMonitorService`보다 `MainActivity.kt`의 상태 계산 함수부터 먼저 볼 것.

## 2026-07-16: 자리비움/퇴근 정책 전면 재정의 — 하루 1회 출퇴근, 초과 자리비움은 근무시간에서 차감

- **경위**: 자리비움 설명을 두 라운드에 걸쳐 다시 주고받으며 사용자가 최종 확정한 정책. 계기가 된 질문: "외부 회의 갔다가 복귀하면, 자리비움 인정 기준(예: 10분)을 넘겨도 이전 퇴근 기록이 지워지나? 아니면 복귀 시 새 출근이 기록되나?" — 기존 코드(자리비움 인정 기준을 넘기면 그날 안에서도 바로 퇴근+재출근 쌍으로 기록)를 정확히 설명했더니, 사용자가 "이상하다"며 정책을 새로 확정: **"하루에 출근/퇴근은 각각 한 번만. 복귀 시에는 그냥 자리비움 해지가 되는 것 — 자리비움 인정 기준이 30분인데 1시간 있다 복귀해도 자리비움 1시간으로 기록되어야 한다."**
- **확정된 새 정책**:
  1. 하루에 ARRIVE/LEAVE는 각각 정확히 1번만 기록됨.
  2. 낮 동안 와이파이가 끊겼다가 같은 날 안에 다시 잡히면 — **끊긴 시간이 자리비움 인정 기준을 넘든 안 넘든 상관없이** — 무조건 자리비움(AWAY)으로 기록되고(실제 전체 지속시간 그대로), 세션은 계속됨. 더 이상 "기준 초과 시 그 자리에서 퇴근 확정" 로직 없음.
  3. 자리비움 인정 기준(분)의 역할이 "퇴근이냐 아니냐를 가르는 기준"에서 "그 자리비움이 근무시간에서 빠지느냐 마느냐를 가르는 기준"으로 바뀜 — 점심시간 차감과 동일한 방식. 기준 미만 자리비움은 근무시간 그대로 인정, 기준 이상 자리비움은 근무시간에서 차감(단, 점심시간과 겹치는 부분은 이중 차감 방지를 위해 그 부분 제외).
  4. 퇴근(LEAVE)은 오직 "자정을 넘겨도 그 단절이 재확인되지 않았을 때"(day-boundary 안전장치)만 기록됨 — 마지막으로 확인된 시각을 기준으로.
- **구현**:
  - `WifiMonitorService.kt`: `checkWifiState()`에서 자리비움 인정 기준·점심시간 유예 로직을 이용한 "당일 즉시 퇴근 확정" 브랜치를 완전히 제거. 재연결 시 무조건 AWAY 기록(지속시간 무관). 그 결과 `isWithinLunchWindow`/`lunchEndTimestampIfPassed`/`lunchWindowMinutes`/`minuteOfDay` 헬퍼 전부 죽은 코드가 되어 삭제, `timestampAtMinuteOfDay` import도 제거. day-boundary 안전장치(자정 넘어도 안 풀리면 `lastSeenAt` 기준 강제 퇴근)는 그대로 유지 — 이게 이제 LEAVE를 만드는 유일한 경로.
  - `WorkStats.kt`: `computeDailyWorkStats`에 `absenceThresholdMinutes: Int` 파라미터 신설. 세션(ARRIVE~LEAVE) 안의 AWAY 이벤트들을 수집해서, 각 AWAY 지속시간이 기준 이상이면 그 시간(점심시간과 겹치는 부분은 `lunchOverlapMinutes`로 빼고) 만큼 근무시간에서 차감. 기준 미만인 AWAY는 그대로 근무시간으로 남음.
  - `CommuteViewModel.kt`: `dailyWorkStats`의 `combine`에 `settingsRepository.absenceThresholdMinutes` 추가(5개 Flow까지는 `combine` 람다 오버로드로 충분).
  - `README.md`: 출퇴근 자동 기록/자리비움 구분/점심시간/근무시간 계산 설명을 새 정책에 맞게 재작성.
- **실기기 검증**: `assembleDebug`+`installDebug` 성공, 강제종료→재실행 크래시 없음, 서비스 정상 기동. 현황 탭에서 "근무중" 상태와 오늘/이번주 근무시간이 정상 표시되는 것 확인(기존 데이터 기준 5시간59분/31시간37분, 회귀 없음). 설정 화면의 자리비움 인정 기준 드롭다운도 정상 렌더링·동작 확인(10분~3시간 11단계, 현재 값 30분 정상 표시).
- **아직 라이브로 재현 못 한 것**: 실제로 기준을 넘는 장시간 이탈 후 복귀했을 때 자리비움으로 기록되고 근무시간에서 정확히 차감되는지는 물리적으로 와이파이 범위를 벗어났다 돌아와야 확인 가능 — adb만으로는 재현 불가, 코드 로직 검토로 갈음.
- **How to apply**: 이 정책 변경으로 `자리비움 인정 기준` 설정의 의미가 근본적으로 바뀌었음("퇴근 트리거"→"근무시간 차감 트리거") — 이후 관련 질문/버그 리포트가 오면 이 새 의미를 기준으로 판단할 것. [[project_gasan_labor_policy]] 계열 문서(다른 대화의 공유 메모리)에도 이 정책 변경을 반영해야 할 수 있으니, 다음에 그 메모리를 다루는 대화가 있으면 이 파일을 참고해 동기화할 것.

## 2026-07-16: 화면 자동회전 토글 관련 — 앱 코드에는 원인 없음, 사용자 답변 대기 중

- **미해결 질문**: "이 프로그램을 설치하면 화면 가로모드 전환 기능이 켜진다, off로만 쓰는데 안 바뀌게 해달라"는 요청에 코드 전수 조사 결과 보고 — 이 앱엔 `Settings.System`/`WRITE_SETTINGS`/`screenOrientation` 등 화면 방향·시스템 설정 관련 코드가 전혀 없고 그런 권한도 없어서, 이 앱이 시스템 자동회전 토글 자체를 켜는 것은 기술적으로 불가능해 보임.
- **제안한 대안**: `MainActivity`를 세로(portrait) 고정하면(매니페스트 `android:screenOrientation="portrait"`), 시스템 자동회전 토글 상태와 무관하게 이 앱 화면만큼은 절대 안 돌아가게 만들 수 있음 — 사용자가 "세로만 쓴다"고 한 것과 맞아떨어지는 안전한 변경.
- **상태**: 사용자가 "세로 고정하자"로 확정 — `AndroidManifest.xml`의 `MainActivity`에 `android:screenOrientation="portrait"` 추가로 구현 완료. `aapt dump badging`으로 매니페스트 병합 결과에 반영된 것 확인(`uses-implied-feature: android.hardware.screen.portrait`), 재빌드·재설치 후 크래시 없음. 시스템 자동회전 토글 자체를 막는 건 아니고(이 앱은 그걸 건드릴 수 없음), 이 앱 화면만 회전 안 하도록 고정한 것.

## 2026-07-16: 현황 탭 그래프에 주말(토·일) 표시/숨김 설정 추가

- **사용자 지시**: "그래프에서 토, 일은 거의 근무하는 날이 아니라 설정에서 토일 보기 기능을 넣어서 평소에는 안 보이다가 필요할 때만 보이게 하자."
- **구현**:
  - `SettingsRepository.kt`: `showWeekend: Flow<Boolean>`(기본값 false) + `setShowWeekend()` 추가.
  - `CommuteViewModel.kt`: `showWeekend: StateFlow<Boolean>` + `setShowWeekend()` 노출.
  - `HomeTabs.kt`: `WeeklyRangeChart`가 `showWeekend`를 받아 `days` 리스트를 7일(`WEEK_DAYS`) 대신 5일(`WEEK_DAYS - 2`)로 자름 — `weekStart`가 항상 월요일이라 앞 5칸이 정확히 월~금. 막대·그리드·요일 라벨·탭 제스처 전부 `days.size` 기반이라 별도 분기 없이 자동으로 5칸짜리로 줄어듦(토·일 칸 자체가 없어지므로 두 칸만큼 남은 요일 칸이 더 넓어지는 부수 효과도 있음).
  - `SettingsScreen.kt`: "주말(토·일) 표시" 카드 신설(`Icons.Filled.Weekend`), `Switch`로 즉시 저장(디바운스 없음, 이 값은 자유 텍스트가 아니라 on/off라 중간값 문제 자체가 없음).
  - `README.md`에 반영.
- **실기기 검증**: 기본 상태(숨김)에서 그래프가 월~금 5칸만 표시(막대가 더 넓어짐), 설정에서 켜면 월~일 7칸 전부 표시, 다시 끄면 5칸으로 복귀하는 것까지 스크린샷으로 확인. `uiautomator dump`로 정확한 스위치 좌표(`checkable="true"` 노드)를 찾아 탭해서 검증(스크린샷 눈대중으로 처음 한 번 빗나갔음 — 점심시간 종료 입력창을 잘못 탭함, 실제 텍스트는 안 바뀌어 데이터 손상 없음). 검증 끝나고 스위치는 다시 기본값(숨김)으로 복원해둠. 크래시 없음.
- **How to apply**: 이 설정은 근무시간 계산(`computeDailyWorkStats`)에는 전혀 영향 없음 — 순수 표시(UI)만 켜고 끄는 것이라, 주말에 실제로 근무했다면 "이번주 총 근무시간"에는 이미 반영되어 있고 그래프에 안 보일 뿐임(의도된 동작).

## 2026-07-16: 그래프 위 안내 문구 제거, 기간 표시로 교체

- **사용자 지시**: "그래프 화면에서 '이번주 출퇴근 시간(...)' 여기 설명 문구는 빼고 '이번주 출퇴근 시간(요기 부분에 기간표시)'로 바꿔줘" — "눌러서 상세 기록, 좌우로 스와이프해 다른 주 보기" 안내 문구를 없애고 그 자리에 실제 날짜 범위를 넣어달라는 요청.
- **구현**: `HomeTabs.kt`의 `weekRangeLabel(weekStart)`(이번주면 "이번주", 과거 주면 "7/6~7/12" 반환하던 함수)를 `weekDateRange(weekStart)`로 이름 바꾸고 항상 "M/d~M/d" 형식의 실제 날짜 범위만 반환하도록 단순화(이번주 여부 분기 제거). 헤더 텍스트를 `"이번주 출퇴근 시간(${weekDateRange(weekStart)})"`로 고정 — 안내 문구는 완전히 삭제.
- **실기기 검증**: 재빌드·재설치 후 "이번주 출퇴근 시간(7/13~7/19)"로 정확히 표시되는 것 확인, 크래시 없음.
- **참고**: "이번주"라는 접두사는 이제 페이지를 넘겨 과거 주를 봐도 고정 텍스트로 남음(괄호 안 날짜만 바뀜) — 사용자가 예시로 준 템플릿을 그대로 따른 것. 만약 과거 주를 볼 때 "이번주"라는 말이 어색하다는 피드백이 오면, 그때는 접두사 자체를 없애고 "출퇴근 시간(날짜)"만 남기는 방향으로 조정할 것.

## 2026-07-16: 설정 화면 설명 문구 전부 제거

- **사용자 지시**: "설정화면에 설명문구가 너무많아. 이거 굳이 설명을 넣을 필요는 없어 너무 잘 아는 내용이다" — 근무 규칙 설정 화면의 각 카드 설명(자리비움 인정 기준, 점심시간, 주말 표시, 근거 문서, 데이터 백업)이 사용자에게는 이미 다 아는 내용이라 불필요.
- **구현**: `SettingsScreen.kt`의 `RuleCard`에서 `description: String` 파라미터 자체를 제거(더 이상 안 받음) — 아이콘+제목+편집 컨트롤만 남김. "근거 문서"/"데이터 백업" 카드(RuleCard 안 쓰고 직접 Card로 만든 것들)의 설명 `Text`도 각각 제거.
- **실기기 검증**: `assembleDebug`+`installDebug` 성공, 재실행 후 logcat에 크래시 없음. 폰 잠금이 풀린 뒤 재설치·재확인 — 설정 화면에 5개 카드(자리비움 인정 기준/점심시간/주말 표시/근거 문서/데이터 백업)가 설명 문구 없이 아이콘+제목+컨트롤만으로 깔끔하게 표시되는 것 스크린샷으로 확인 완료.
- **How to apply**: 이 사용자는 자신이 오랫동안 함께 설계해온 규칙(자리비움/점심시간/주말표시 등)에 대해 이미 충분히 숙지하고 있어서, 설명형 UI 문구를 반기지 않음 — 앞으로 새 설정 항목을 추가할 때도 기본적으로 설명 문구 없이 제목+컨트롤만 넣고, 정말 처음 보는 사용자에게 필요한 수준의 문구가 아니면 굳이 추가하지 말 것.

## 2026-07-16: 이전에 고쳤다고 했던 1분짜리 자리비움이 다시 발생 — 근본 원인이 달랐음, 2연속 미스 디바운스로 완전히 제거

- **사용자 지시**: "지금도 1분 자리비움 기록이 보이는데. 이거 수정했다고 했잖아!!!" — 오전에 고쳤다고 보고한 스캔 캐시 미스 버그와 겉보기엔 같은 증상(1분짜리 자리비움)이 또 발생, 강한 불만 표출.
- **조사**: DB를 직접 열람해 실제 기록 확인 — `id=12, AWAY, 2026-07-16 12:39:40~12:40:40, 정확히 1분(폴링 주기와 일치)`. 이 시점은 이미 오전에 배포한 `isCompanyWifiNearby`(연결 상태 OR 조건) 수정본이 설치되고 한참 뒤였음(`lastUpdateTime`으로 확인) — 즉 오전 수정은 여전히 유효했는데도 또 발생. `dumpsys wifi`의 이벤트 로그·logcat 버퍼가 모두 그 시점(12:39)까지 도달 못 하고 롤오버되어(각각 6/25, 14:00부터 시작) 직접적인 원인 로그는 못 찾음.
- **원인 재해석**: 오전 수정은 "스캔 캐시만 갱신 안 됐고 실제 연결(`WifiInfo.connectionInfo`)은 멀쩡했던 경우"만 잡는 수정이었음. 이번 건은 그 조건(연결 정보 OR 스캔 캐시)까지 포함해서 딱 한 틱 동안 진짜로 "연결 안 됨"으로 보였다는 뜻 — AP 간 재연결(로밍), DHCP 갱신, 순간적인 재결합처럼 실제 물리 계층에서 아주 짧게 끊기는 이벤트로 추정(60초 폴링 해상도로는 이런 찰나의 이벤트를 감지 자체는 피할 수 없음). 즉 오전 수정이 틀린 게 아니라, **애초에 다른 실패 유형**이었음.
- **수정**: 감지 로직 자체에 "2연속 미스 디바운스"를 추가 — 이제 와이파이가 안 잡히는 첫 폴링 틱에서는 바로 `awaySinceAt`을 세우지 않고 `provisionalAwaySinceAt`(잠정 미스 시각)만 기록. 바로 다음 틱에도 또 안 잡히면 그때 비로소 `provisionalAwaySinceAt`을 진짜 `awaySinceAt`으로 승격(시작 시각은 소급 적용해 실제 이탈 시간이 정확하게 유지됨). 그 사이에 다시 잡히면 `provisionalAwaySinceAt`만 조용히 지우고 **아무 기록도 안 남김** — 찰나의 블립은 아예 자리비움으로도 기록되지 않게 됨.
  - `SettingsRepository.kt`: `provisionalAwaySinceAt: Flow<Long?>` + `setProvisionalAwaySinceAt`/`clearProvisionalAwaySinceAt` 추가.
  - `WifiMonitorService.kt`: `checkWifiState()`의 "안 잡히는데 근무중" 분기를 2단계 디바운스로 재작성. 재연결 시(ARRIVE든 재복귀든) 항상 `clearProvisionalAwaySinceAt()`도 같이 호출해 잠정 상태가 안 남게 함. 날짜 경계 안전장치(자정 넘김 강제 퇴근) 분기에도 동일하게 clear 추가(방어적).
- **부수 효과**: 이 디바운스 때문에 진짜 이탈이 시작될 때도 감지 시작이 최대 폴링 주기(60초)만큼 늦어질 수 있음 — 하지만 실제 퇴근/자리비움 확정 기준(수십 분 단위)에 비하면 무시 가능한 오차. 반대로 순수하게 1틱 만에 resolve되는 진짜 아주 짧은(약 30~50초) 자리비움은 이제 아예 기록에서 빠짐 — 사용자가 원한 "잡음 안 보이게"와 일치.
- **데이터 정리**: 이미 기록됐던 `id=12`(가짜 1분 자리비움)는 세션 앞부분에서 쓰던 것과 동일한 방식(run-as로 db pull → 로컬 sqlite3로 DELETE+체크포인트 → base64+stdin으로 앱에 직접 스트리밍, scoped storage 우회)으로 직접 삭제하고 정상 데이터만 남은 것 확인.
- **실기기 검증**: 재빌드·재설치·재실행 크래시 없음, 서비스 정상 기동 확인. 디바운스가 실제로 찰나의 블립을 걸러내는지는 물리적으로 재현해야 확인 가능해서(이번에도 adb만으론 재현 불가) 코드 로직 검토로 갈음.
- **How to apply**: 같은 증상(1분 자리비움)이라도 원인이 매번 같으리라 가정하지 말 것 — 이번엔 시간 로그가 전부 롤오버돼서 직접 증거를 못 찾았지만, "지금 실행 중인 빌드에 이전 수정이 실제로 포함돼 있는가"(`dumpsys package`의 `lastUpdateTime`으로 확인 가능)부터 항상 먼저 검증하고, 포함돼 있는데도 재발했다면 이전 수정이 커버 못 하는 **다른 경로**가 있다고 보고 접근할 것. 폴링 기반 감지 시스템에서 "찰나의 노이즈"를 근본적으로 없애는 가장 확실한 방법은 특정 실패 유형 하나하나를 패치하는 게 아니라 **N연속 확인** 같은 디바운스를 감지 로직 자체에 넣는 것.

## 2026-07-16: 점심시간 입력도 텍스트 대신 시계(TimePicker) 컴포넌트로 교체

- **사용자 지시**: "점심시간 입력도 시계컴포넌트로 하자.." — 자리비움 인정 기준(드롭다운)처럼 점심시간 시작/종료도 자유 텍스트(HH:mm 직접 입력) 대신 안드로이드 기본 TimePicker로 바꿔달라는 요청. `EditEventDialog.kt`에서 기록 시각 수정할 때 이미 쓰던 것과 같은 컴포넌트.
- **구현**:
  - `EditEventDialog.kt`의 `PickerField`/`TimePickerDialog`(원래 `private`)를 그대로 재사용하려고 `private` 제거(같은 모듈이라 파일 간 공유 가능). `PickerField`에 `modifier: Modifier = Modifier` 파라미터 추가(점심시간 화면에서 시작/종료 두 필드를 `Row` 안에 `weight(1f)`로 나란히 배치해야 해서).
  - `SettingsScreen.kt`의 `LunchWindowEditor`를 전면 재작성 — 자유 텍스트 `OutlinedTextField` 2개 + 800ms 디바운스 자동저장 + `DisposableEffect` 강제 플러시 조합을 걷어내고, `PickerField` 2개(탭하면 `TimePickerDialog` 오픈) + `rememberTimePickerState`로 교체. 확인 누르면 그 즉시 `onSave(newStart/newEnd, 상대방 값)` 호출(시작<종료 검증은 그대로 유지) — 자리비움 인정 기준 드롭다운과 동일하게 디바운스 없는 즉시 저장 패턴으로 통일.
  - 이제 안 쓰는 `parseHHmmToMinuteOfDay`(`TimeOfDay.kt`)와 `AUTO_SAVE_DEBOUNCE_MS`, 관련 import(`DisposableEffect`/`LaunchedEffect`/`kotlinx.coroutines.delay`/`KeyboardOptions`/`KeyboardType`) 전부 정리(죽은 코드 삭제 — 다른 곳에서 안 쓰는 것 grep으로 재확인 후 제거).
- **실기기 검증**: 재빌드·재설치·재실행 크래시 없음. 점심시간 카드에 시계 아이콘이 달린 시작/종료 필드로 바뀐 것, 탭하면 실제 시계형 TimePicker가 기존 값(11:20)으로 초기화되어 뜨는 것 스크린샷으로 확인.
- **How to apply**: 이 세션에서 자유 텍스트+디바운스 패턴(자리비움 인정 기준, 점심시간 둘 다 원래 이 방식이었음)이 "값이 조용히 유실될 수 있다"(자리비움 기준 30→10 되돌아간 사건 참고)는 이유로 계속 피커 방식으로 교체되고 있음 — 이 앱에 남아있는 다른 자유 텍스트 설정 입력이 있다면, 굳이 요청 없어도 같은 이유로 피커/드롭다운 전환을 먼저 제안해볼 만함.

## 2026-07-16: 자리비움 정책 재수정 — "자리비움 = 지정 시간 이상 인식 안 됨"이 정의 자체, 미만이면 기록 자체가 없어야 함

- **사용자 지시**: "문제는 자리비움이 1분은 없다는거야. 자리비움은 지정된 시간 이상 인식이 안될때라는거야." — 바로 앞서 만든 "2연속 미스 디바운스"는 찰나의 노이즈(30~60초)만 걸러내고, 그 사이 길이(예: 3~25분, 기준 30분 미만)의 진짜 이탈은 여전히 자리비움 이벤트로 **기록**되고 있었음(근무시간 계산에서만 차감 안 될 뿐). 사용자는 이걸 원한 게 아니었음 — "자리비움"이라는 **기록 자체**가 기준 시간 미만이면 아예 존재하면 안 된다는 것.
- **이전 정책과의 차이**: 오늘 세션 초반에 "자리비움이 30분 기준인데 1시간 있다 복귀하면 자리비움이 1시간으로 기록돼야 한다"는 지시를 "모든 재연결은 지속시간과 무관하게 무조건 자리비움으로 기록하고, 근무시간 계산 단계에서만 기준으로 차감 여부를 가른다"로 구현했었음 — 그런데 이번 지시로 "기록 여부"와 "차감 여부"가 사실 같은 기준으로 판단돼야 한다는 게 명확해짐. 즉 자리비움 인정 기준은 "기록할지 말지"와 "차감할지 말지"를 동시에 결정하는 하나의 경계선.
- **수정**: `WifiMonitorService.kt`의 재연결 분기 — 이제 재연결 시 `now - awaySince >= 자리비움 인정 기준(분)`일 때만 `recordEvent(AWAY, ...)`와 "자리비움 종료" 알림을 호출. 기준 미만이면 `awaySinceAt`만 조용히 지우고(`clearAwaySinceAt()`) **아무 것도 기록하지 않음** — 마치 끊긴 적이 없었던 것처럼 완전히 무시됨. 클래스 상단 KDoc도 이 최종 정의("자리비움은 기준 시간 이상 이탈이어야만 존재")로 다시 정리.
- **2연속 미스 디바운스는 그대로 유지**: 이번 수정과 별개로 여전히 유효 — 찰나의 폴링 노이즈가 애초에 `awaySinceAt` 타이머 자체를 시작시키지 않게 막는 1차 방어선이고, 이번 수정은 "타이머가 시작은 됐지만 기준을 못 채우고 끝난" 경우를 다시 한번 걸러내는 2차 방어선. 둘이 겹쳐도 문제없음(디바운스가 이미 막아준 사례에 대해 이 체크는 그냥 항상 통과).
- **`WorkStats.kt`는 안 건드림**: 자동 감지분은 이제 구조적으로 항상 기준 이상만 기록되니 그 안의 "기준 미만이면 차감 안 함" 방어 로직은 사실상 자동 감지 경로에서는 트리거될 일이 없어졌지만, "빠진 기록 추가" UI로 사용자가 수동으로 짧은 자리비움을 입력하는 경우엔 여전히 유효하게 작동해야 해서 그대로 둠.
- **실기기 검증**: 재빌드·재설치·재실행 크래시 없음, DB에 남아있는 AWAY 기록 전수 조회 결과 현재는 0건(이전 잘못된 1분짜리는 이미 삭제된 상태) — 새 코드가 배포된 시점 이후 아직 새로 재현될 실제 이탈 이벤트가 없어서, 기준 미만 이탈이 실제로 기록을 안 남기는지는 이번에도 물리적 재현이 필요해 코드 검토로 갈음.
- **How to apply**: "기록 안 남음"과 "근무시간 차감 안 함"은 사용자 입장에서 서로 다른 개념이 아니라 하나였음 — 앞으로 비슷하게 "이 이벤트가 근무시간에 반영되는 기준"과 "이 이벤트가 애초에 기록되는 기준"을 분리해서 구현하기 전에, 사용자가 정말 그 둘을 별개로 원하는지 먼저 확인할 것. 이번처럼 "당연히 같은 기준일 것"이라고 사용자가 여기는 경우가 흔할 수 있음.

## 2026-07-20(세션 재개, 4일 만): 기록 탭이 "이번주만" 보여주던 걸 전체 기간 + 날짜 필터로 교체

- **경위**: 새 대화 시작 시점(월요일 08:2x)에 서비스가 또 죽어있었음(watchdog은 앱을 직접 열어야만 작동하는 한계 — 주말 내내 앱을 안 열어서 무방비 상태였던 것으로 추정). 앱을 여니 즉시 되살아나 오늘 출근(08:30)이 정상 기록됨. 이 과정에서 "지난주 목요일(7/16) 퇴근 기록이 안 잡혀 있다"는 사용자 지적이 나왔는데, DB를 직접 열어보니 7/16 출근 07:43→퇴근 17:20:54가 실제로는 정상 기록돼 있었음 — **문제는 데이터가 아니라 UI**: 기록 탭이 `weekEvents`(이번주 월요일 이후 것만)를 보여주는 구조라, 주가 바뀌면 지난주 기록이 안 보였던 것.
- **사용자 지시**: "기록리스트에 이번주만 보이는게 아니다. 전체가 보이게 하자. 검색기간을 넣어 기간 검색이 되게 할 거야."
- **구현**:
  - `EditEventDialog.kt`의 `localMidnightToUtcMillis`/`utcMillisToLocalMidnight`(DatePicker의 UTC-자정 표현과 로컬 자정 타임스탬프 상호 변환 헬퍼, 원래 `private`)를 `PickerField`/`TimePickerDialog`처럼 `private` 제거해서 재사용 가능하게 함.
  - `HomeTabs.kt`의 `RecordsTab`: `filterStart`/`filterEnd`(둘 다 nullable `Long`, 기본값 null="전체") 상태 추가. 상단에 `PickerField` 2개(시작일/종료일, 탭하면 표준 Material3 `DatePickerDialog` 오픈) + 필터가 걸려 있을 때만 보이는 "전체" 리셋 `TextButton`. 목록은 `events.filter { day -> (filterStart==null || day>=filterStart) && (filterEnd==null || day<=filterEnd) }`로 필터링 후 시각 내림차순 표시. 빈 상태 문구도 "기록이 없습니다"/"이 기간에 기록이 없습니다"로 분기.
  - `RecordsTab`이 받는 `events` 파라미터 자체를 `weekEvents`(주간 필터)에서 `allEvents`(전체)로 교체 — `MainActivity.kt`의 `RecordsTab(events = weekEvents, ...)` → `events = allEvents`로 변경.
  - 이제 안 쓰는 `CommuteViewModel.weekEvents`(StateFlow)와 `MainActivity.kt`의 `weekEvents` 수집 코드 삭제(죽은 코드 정리).
  - `README.md`의 "기록" 탭 설명을 "전체 이벤트 스크롤 목록, 상단 시작일/종료일 DatePicker로 기간 필터"로 갱신.
- **실기기 검증**: 재빌드·재설치·재실행 크래시 없음. 기록 탭에 7/13부터 오늘(7/20)까지 전체가 다 보이는 것, 시작일을 7/16으로 선택하면 그 이후 것만 필터링되는 것, "전체" 버튼으로 리셋되는 것까지 스크린샷으로 전부 확인. `uiautomator dump`로 정확한 필드 좌표 찾아서 탭(스크린샷 눈대중 좌표가 처음엔 빗나감 — PickerField가 `enabled=false`라 접근성 트리 좌표 판단이 헷갈렸음, 두 번째 시도에 성공).
- **별도로 발견(수정은 안 함, 기록만)**: 로그캣에 이틀 전(7/18) 위치 권한 관련 `SecurityException` 크래시가 있었음(`FOREGROUND_SERVICE_LOCATION`/위치 권한이 그 순간엔 없었던 것으로 보임) — 지금은 권한 다 정상이고 재현 안 됨이라 이번엔 손 안 댐. 서비스가 "앱을 열어야만" 되살아나는 한계(watchdog)와 겹쳐서, 권한이 잠깐이라도 빠지면 며칠씩 무방비 상태가 될 수 있다는 걸 시사 — 다음에 관련 이슈 나오면 이 항목부터 볼 것.
- **How to apply**: "이번주만 보여준다"처럼 스코프를 좁혀둔 UI는, 사용자가 나중에 지난 기록을 찾다가 "빠졌다"고 오인할 수 있다는 걸 이번에 배움 — 데이터가 맞는지부터 항상 먼저 확인하고(이번처럼 DB를 직접 까봐야 UI 문제인지 데이터 문제인지 구분 가능), UI 스코프 제한이 원인이면 그 제한 자체를 없애거나 최소한 명확히 표시할 것.

## 2026-07-20: SSID 이름만 보던 감지를 BSSID(AP 하드웨어 주소) 기반으로 교체 — 오검출의 진짜 근본 원인

- **사용자 지적**: "지난주 금요일에는 출근을 안했는데 왜 출퇴근 기록이 있지?? iptime5G라는게 ssid가 좀 흔한 검출이 되는 것이라... 그냥 이름으로만 확인하는게 아닌가 싶다." — 정확한 진단이었음.
- **확인한 사실**: 7/17(금) 기록이 ARRIVE 13:18 / LEAVE 16:30로, 정상 출근 패턴이 아님(오후 1시 출근). 실기기 스캔 결과 확인 시 회사 AP는 `iptime5G` / BSSID `58:86:94:03:a8:92` 단 하나. `iptime5G`는 국내 공유기 기본 이름이라 다른 건물에도 흔함 — 사용자가 금요일에 있던 다른 장소의 동명 AP를 회사로 오인해 **출퇴근 한 쌍이 통째로 가짜 기록**된 것.
- **왜 이전 수정들로 못 잡혔나**: 지금까지의 모든 수정(스캔 캐시 미스 → 연결 상태 OR 조건, 2연속 미스 디바운스, 자리비움 기준 게이트)은 전부 "회사 AP가 맞다는 전제 하에 언제 기록할지"를 다뤘음. 정작 **"이게 회사 AP가 맞는가"** 자체는 SSID 문자열 비교 한 줄이었고 아무도 의심하지 않았음. 사용자가 짚어주기 전까지 이 층위를 못 본 게 이번 세션 최대의 맹점.
- **구현**:
  - `WifiUtils.kt`: `isCompanyWifiNearby(context, companySsid, companyBssids: Set<String>)`로 시그니처 변경 — BSSID 집합이 비어있지 않으면 SSID 일치 **AND** BSSID 일치를 요구(연결 정보/스캔 결과 양쪽 모두). 비어있으면 기존 SSID-only로 폴백(구버전 등록 사용자가 갑자기 아무것도 감지 못 하는 것보다 낫다고 판단). `normalizeBssid()`(소문자 통일 — OS가 `connectionInfo`와 `scanResults`에서 hex 대소문자를 일관되게 주지 않음), `nearbyBssidsFor(context, ssid)`(해당 SSID를 쏘는 현재 보이는 모든 AP의 BSSID 수집) 추가.
  - `SettingsRepository.kt`: `companyBssids: Flow<Set<String>>`(`stringSetPreferencesKey`) + `setCompanyBssids()`.
  - `CommuteViewModel.kt`: `registerCompanySsid()`가 등록 시 `nearbyBssidsFor()`로 BSSID도 같이 저장. `addNearbyCompanyBssids()`(현재 보이는 AP를 기존 집합에 병합, 사무실 AP가 여러 대이거나 등록 당시 범위 밖이었던 경우용), `removeCompanyBssid()` 추가.
  - `SettingsScreen.kt`: "회사 AP 등록 (N대)" 카드 신설 — 등록된 BSSID 목록 + 개별 삭제, "지금 보이는 {SSID} AP 등록" 버튼, 0대일 때는 빨간 경고 문구.
  - `MainActivity.kt`/`WifiMonitorService.kt`: 두 호출부 모두 `companyBssids` 전달하도록 수정(`LaunchedEffect` key에도 추가).
- **왜 단일 BSSID가 아니라 집합인가**: 사무실은 보통 같은 SSID로 AP를 여러 대 운영하므로, AP 하나에만 고정하면 로밍이 "이탈"로 잡힘. 집합으로 두고 필요할 때 추가하는 방식이 안전.
- **실기기 검증**: 재빌드·재설치 후 설정 화면에 "회사 AP 등록 (0대)" + 경고 문구 정상 표시(기존 등록분에는 BSSID가 없으므로 폴백 상태가 맞음) → "지금 보이는 iptime5G AP 등록" 탭 → 실제 회사 BSSID `58:86:94:03:a8:92` 1대가 정확히 등록되고 "회사 AP 1대 등록됨" 토스트 확인 → 홈 화면 복귀 시 상태 카드가 여전히 "근무중"으로 유지되는 것 확인(= BSSID 매칭이 실제 회사 AP를 제대로 인식). 크래시 없음, 서비스 정상 기동.
- **오염 데이터 정리**: 7/17의 가짜 ARRIVE(id=14)/LEAVE(id=16) 2건을 기존 절차(run-as pull → 로컬 sqlite3 DELETE+체크포인트 → base64+stdin 스트리밍)로 삭제. 나머지 7/13~7/16 및 오늘 7/20 08:30 출근 기록은 전부 온전한 것 확인.
- **How to apply**: 감지 로직에서 반복적으로 오검출이 나오면, "언제/어떻게 기록할지"(타이밍·디바운스·임계값)를 계속 조정하기 전에 **"무엇을 식별자로 쓰고 있는지"** 를 먼저 의심할 것. 이번엔 타이밍 계열 수정을 세 번이나 한 뒤에야 식별자 자체(SSID 문자열)가 근본 원인이었음이 드러났음. 사용자가 도메인 지식("이 이름 흔하다")으로 짚어준 게 결정적이었으니, 비슷하게 "왜 이런 게 잡히지?"류 질문이 오면 식별 기준부터 되짚을 것.

## 2026-07-20: 랄프루프 5회 감사 (워커=Opus 병렬) — 정적 리뷰만으로는 못 잡던 실제 결함 7건 발견·수정

- **사용자 지시**: "지금 워커 모델을 opus로 바꾸었다. 랄프루프 5회 돌려 구현 내용 점검 및 기능 검토를 실시해라."
- **감사 범위**: 직전 코드 랄프루프(`575ed6b`) 이후 14개 커밋 + 미커밋 BSSID 작업. `git diff 575ed6b HEAD --stat`으로 범위 확정([[이전 랄프루프 항목]]의 "직전 랄프루프 커밋부터 diff" 방식 재사용).
- **Round 1 (정적 리뷰, 워커 3대 병렬)**: 감지 로직(WifiUtils/WifiMonitorService/SettingsRepository), 근무시간 계산(WorkStats/TimeOfDay), Compose UI(MainActivity/HomeTabs/SettingsScreen/EditEventDialog)로 영역을 나눠 Agent 3대를 동시에 돌림. 각 워커에 "구체적 실패 시나리오와 file:line이 있는 진짜 결함만, 스타일 지적 금지"를 명시한 게 핵심 — 추상적 코드리뷰가 아니라 재현 가능한 버그만 올라옴.
- **발견·수정한 결함**:
  1. **(HIGH) 자리비움 라벨이 영원히 안 바뀜** — `commuteStatus()`가 컴포지션 중 `System.currentTimeMillis()`를 직접 읽는데, 와이파이가 끊긴 뒤엔 매 폴링이 동일 값을 써서 `CompactStatusCard`(전부 primitive 파라미터라 skippable)가 재구성되지 않음. 임계값이 지나도 "근무중"에 멈춰 서비스가 기록한 AWAY와 화면이 불일치. → `nowTick` 상태를 폴링 루프에서 갱신해 `nowMillis` 파라미터로 주입, `commuteStatus`가 그 값을 쓰도록 변경. **이번 세션에 내가 직접 구현한 기능이 실제로는 작동하지 않고 있었음** — 당시 "코드 로직 검토로 갈음"하고 라이브 검증을 못 한 게 그대로 구멍이 됐음.
  2. **(HIGH) AWAY 구간이 세션 범위로 클램프되지 않아 이중 차감** — `rawMinutes`는 07:00부터 재는데 AWAY는 원본 구간 전체를 차감. 06:00 출근 + 06:05~06:50 자리비움이면 45분이 통째로 사라짐(600분이어야 할 게 555분). LEAVE 이후까지 뻗은 AWAY는 과다 차감. 워커 2대(감지·계산)가 **독립적으로 같은 결함**을 지목.
  3. **(MED) 겹치는 AWAY 구간이 합집합이 아니라 단순 합산** — 14:00~15:00과 14:30~15:30이면 90분이어야 할 게 120분 차감.
  4. **(MED) 자정 넘는 세션에서 점심 차감 기준일 불일치** — 세션 레벨은 `recognizedStart`의 날짜, AWAY 레벨은 각 AWAY 자신의 날짜로 점심 창을 계산해서, 차감된 적 없는 점심을 되돌려주는 경로가 생김.
  5. **(MED) 이벤트 insert가 상태 쓰기보다 먼저라 중복 삽입 위험** — LEAVE insert 후 `setIsAtWork(false)` 전에 코루틴이 취소되면(모니터링 토글 때마다 `stopService` 호출됨) 매 60초 폴링마다 동일 LEAVE가 무한 재삽입. AWAY도 같은 구조. → **상태 먼저, insert 나중**으로 순서 반전(취소 시 이벤트 1건 유실은 기록 누락 배너로 복구 가능하지만, 무한 삽입은 복구 불가라는 트레이드오프).
  6. **(MED) `isAtWork=true` + `lastSeenAt=null`이면 앱이 영구 정지** — day-boundary 안전장치가 `lastSeen != null`을 요구해서 세션이 영원히 안 닫히고, 그 결과 이후 어떤 출근도 기록되지 않음. → `setLastSeenAt(now)`를 분기 최상단으로 올려 "isAtWork면 lastSeenAt 존재" 불변식을 보장 + 기존 손상 상태를 자가 복구하는 리셋 경로 추가.
  7. **(MED) `02:00:00:00:00:00` placeholder BSSID 미필터** — 위치 권한이 degrade된 상태에서 등록하면 이 가짜 주소가 저장되고, 집합이 비어있지 않으니 SSID 폴백도 안 되어 **회사가 영원히 감지 안 됨**. → `isUsableBssid()`로 all-zero와 함께 필터. 추가로 BSSID 캡처가 0건이면 조용히 이름 기반으로 퇴화하지 않도록 Toast 경고(이게 바로 7/17 오검출의 원인이었던 상태).
  - 부수: `rawSpanMinutes`("점심 포함" 타일)가 장시간 자리비움까지 "present"로 계산하던 것 → `minutes + lunchMinutes`로 정정. `commuteStatus` KDoc의 "3-second poll"(실제 60초) 정정. 기록 탭 빈 상태 가드가 `missingRecords`까지 AND로 묶여 있어 필터 결과 0건일 때 안내문 없이 빈 리스트만 보이던 것 정정.
- **의도적으로 안 고친 것**: 22:00 그리드라인 미표시(이전 세션에서 "사소한 비대칭, 과잉 엔지니어링 지양"으로 명시적으로 남긴 결정 — 워커가 결함으로 올렸지만 기존 결정 존중). 자정 넘겨 근무하는 세션이 다음날 출근을 큰 AWAY로 흡수하는 동작(사용자가 확정한 "하루 1회 출퇴근" 정책과 오히려 일치하고, 피해 크기는 클램프 수정으로 이미 해소됨).
- **Round 2**: `clean assembleDebug` 전체 회귀 빌드 40/40 성공. **Round 3**: `compileDebugKotlin --rerun-tasks` 경고 0건(미사용 import/변수 없음). **Round 4**: WorkStats 산술을 실제 JDK로 독립 검증 — 수정 전 버그 4종(07:00 이전 자리비움/07:00 걸친 자리비움/LEAVE 이후까지 뻗은 자리비움/겹치는 자리비움)과 정상 3종(1시간 외부회의·기준 미만 단시간·점심시간 내 자리비움)까지 **7케이스 전부 기대값 일치**. **Round 5**: 실기기 재설치·재실행 — 크래시 없음, 서비스 정상 기동, BSSID 등록(`58:86:94:03:a8:92`) 유지, 근무시간 정상 누적 확인.
- **How to apply**: (1) 이번에 가장 값진 발견(자리비움 라벨 미갱신)은 **"내가 직접 구현하고 코드 검토로 갈음했다"고 메모에 적어둔 바로 그 지점**이었음 — 라이브 검증을 못 하고 넘어간 항목은 반드시 나중에 별도로 파헤칠 것. (2) 워커에게 영역을 나눠주되 "구체적 실패 시나리오 없으면 보고하지 말라"고 제약을 거는 게 노이즈를 크게 줄임. (3) 워커 보고를 그대로 믿지 말고 직접 코드를 다시 읽어 확인할 것 — 실제로 22:00 그리드라인은 과거의 의도적 결정이었고, 자정 세션 건은 정책상 정상 동작이었음(둘 다 "결함"으로 올라왔지만 수정하지 않는 게 옳았음). (4) 순수 계산 로직은 실기기 없이도 JDK로 독립 검증 가능 — 물리적 재현이 어려운 산술 버그는 이 방법을 우선할 것.

## 2026-07-20: 그래프 요일 라벨 아래에 출근 시각 상시 표시

- **사용자 지시**: "출퇴근 그래프에서 그래프 하단에 작은 글씨로 막대그래프 폭을 넘지않게 출근시간을 시:분으로 출력하게 하자. 눌러서 볼게 아니라 아예 나오게 하면 한번에 알수 있어 용이할거 같다."
- **구현**: `HomeTabs.kt`의 `WeeklyRangeChart` 하단 요일 라벨 `Row`에서, 각 요일 셀을 `Text` 하나에서 `Column`(요일 라벨 + 출근 시각)으로 변경. 출근 시각은 `labelSmall.copy(fontSize = 9.sp)` + `maxLines = 1` + `softWrap = false`로, 요일 라벨보다 한 단계 작게. 기록이 없는 날은 `statByDay[day]?.firstArriveAt?.let { }`로 아예 렌더링 안 함(빈 칸).
- **어떤 값을 보여줄지 판단**: `stat.firstArriveAt`(07:00 클램프된 값, 막대를 그리는 데 쓰는 바로 그 값)을 사용 — 막대가 시작하는 위치와 라벨 숫자가 항상 일치해야 혼란이 없기 때문. 06:00에 출근해도 막대는 07:00에서 시작하는데 라벨만 06:00이면 어긋나 보임. 실제 원본 출근 시각은 기존대로 막대를 눌러 여는 `DayDetailDialog`에서 확인 가능(투명성은 그쪽에서 유지 — [[2026-07-15 07:00 클램프 항목]]의 설계 방침 그대로).
- **폭 검증**: 사용자가 "막대 폭을 넘지 않게"를 요구했으므로, 칸이 가장 좁아지는 **7일(주말 표시 ON) 레이아웃**에서 별도로 확인 — 설정에서 주말 표시를 켜서 7칸으로 만든 뒤 `08:30`이 칸 안에 들어가고 이웃 요일과 겹치지 않는 것을 스크린샷으로 확인, 검증 후 주말 표시는 원래 기본값(숨김)으로 되돌려놓음. 5칸 레이아웃도 정상.
- **실기기 검증**: 재빌드·재설치 후 월요일 막대 아래 `08:30` 정상 표시, 크래시 없음. `README.md`에도 반영.
- **How to apply**: 이 사용자는 "폭을 넘지 않게" 같은 레이아웃 제약을 명시적으로 말하는 편 — 그런 요구가 나오면 가장 여유로운 상태가 아니라 **가장 빡빡한 상태**(여기선 7칸)를 일부러 만들어서 확인할 것. 확인용으로 설정을 바꿨으면 반드시 원래 값으로 복원할 것.

## 2026-07-20(후속): 출퇴근 시각을 X축 라벨이 아니라 막대 기준 위치로 이동

- **사용자 지시**: "출근은 막대그래프 하단, 퇴근은 막대그래프 상단으로 표시가능해? 지금은 x축의 라벨아래인데 이게 아니라 그래프 위치기준으로.." — 직전 구현(요일 라벨 아래 Column으로 출근 시각 표시)을 되돌리고, 캔버스 안에서 각 막대의 실제 위치를 기준으로 그리라는 요청. 퇴근 시각도 함께 추가.
- **구현**: `HomeTabs.kt` — 요일 라벨 `Row`는 원래의 단순 `Text` 하나로 복원. 대신 `Canvas`의 막대 그리기 루프 안에서 `textMeasurer.measure()` + `drawText()`로 직접 그림. 출근은 막대 아래쪽 끝(`bottom + labelGap`), 퇴근은 막대 위쪽 끝(`top - 텍스트높이 - labelGap`)에 배치하고 막대 중앙 정렬. `barTimeStyle`(labelSmall + 9.sp)로 축 라벨보다 한 단계 작게 해서 축과 경쟁하지 않게 함.
- **크래시 예방**: 좌표 클램프의 상한을 반드시 `coerceAtLeast(0f)`로 먼저 보정한 뒤 `coerceIn(0f, max)`에 넘김 — 이 차트는 과거에 정확히 이 패턴(`coerceIn(0f, 음수)`)으로 크래시 루프를 낸 적이 있어서 동일 실수를 반복하지 않도록 의도적으로 같은 방어를 적용.
- **근무 중인 날 처리**: `stat.open`이면 막대 위쪽은 "현재 시각"이지 퇴근이 아니므로 위쪽 라벨을 아예 그리지 않음 — 아직 퇴근하지 않은 시각을 퇴근으로 표시하면 오해를 부름.
- **실기기 검증**: 이번주(오늘 진행 중)에서는 막대 아래 `08:30`만 표시되고 위쪽 라벨 없음(의도대로). 지난주로 스와이프해 완료된 세션 4일치에서 아래 출근(09:55/07:36/07:55/07:43)·위 퇴근(17:18/16:35/20:12/17:20)이 모두 정확히 표시되고 서로 겹치지 않는 것 스크린샷으로 확인. 확인 후 "이번주로" 버튼으로 복귀. 크래시 없음. `README.md`도 갱신.
- **How to apply**: 직전 턴에 "요일 라벨 아래"로 구현했다가 한 턴 만에 "막대 기준"으로 뒤집힌 케이스 — "그래프 하단"이 축 라벨 영역인지 막대 아래인지 모호했음. 다음에 차트 상의 위치 지시를 받으면 "축 기준인지 요소(막대) 기준인지"를 먼저 확인하면 왕복을 줄일 수 있음.

## 2026-07-20: 랄프루프 10회 전체 코드 감사 (워커 4대 병렬) — 크래시 원인 규명 + 백업 데이터 손실 결함 발견

- **사용자 지시**: "전체 코드 다시 랄프루프 스킬로 10회 돌려서 문제 검토하고. 내용을 보강하자" — 직전 감사가 diff 범위였던 것과 달리 이번엔 전체 21개 파일 3,328줄 대상.
- **워커 분담(4대 병렬)**: ①영속성/백업(Room·DataStore·BackupData) ②서비스 수명주기/권한/매니페스트/Gradle ③Compose UI 상태/네비게이션/스레딩 ④도메인 규칙 정합성(README 대조 포함). 각 워커에 "구체적 실패 시나리오+file:line 없으면 보고 금지"를 명시.
- **최대 성과 — 7/18 실제 크래시의 정확한 원인 규명**: 워커 3대가 **독립적으로 동일 결론**에 도달. `ACCESS_FINE_LOCATION` 보유는 location-type FGS 시작의 **필요조건일 뿐 충분조건이 아님** — Android 14+는 "while-in-use eligible state"를 추가로 요구하고, 이건 `checkSelfPermission`으로 사전 확인이 불가능. 게다가 배터리 최적화 예외는 *background-FGS-start* 면제일 뿐 while-in-use 자격과는 **다른 게이트**라 이 크래시를 못 막음. 결정적으로 `LaunchedEffect`는 컴포지션 수명이라 **앱이 백그라운드로 가도 60초 루프가 계속 돌며** `startForegroundService`를 호출 → 정확히 금지된 상황. START_STICKY라 크래시→재시작→재크래시 루프.
  - **수정**: ⓐ`onStartCommand`의 `startForeground`를 try/catch로 감싸 실패 시 `stopSelf()`+`START_NOT_STICKY`, ⓑ`WifiMonitorService.start()` 호출부도 try/catch(호출자 쪽에서 던지는 `ForegroundServiceStartNotAllowedException` 대응), ⓒ워치독 루프를 `lifecycleOwner.repeatOnLifecycle(STARTED)`로 감싸 백그라운드에선 아예 안 돌게 함(불필요한 WifiManager 바인더 호출도 같이 제거).
- **백업이 데이터를 조용히 잃던 문제**: `BackupSettings`가 `Keys` 목록과 연동돼 있지 않아, 나중에 추가된 `companyBssids`와 `showWeekend`가 **백업에 아예 포함되지 않음**. 복원하면 회사 SSID만 살아나고 BSSID 집합이 비어 → `isCompanyWifiNearby`가 이름 기반 폴백으로 떨어짐 = **7/17 가짜 출퇴근을 만든 바로 그 상태로 조용히 회귀**. 백업 필드 추가 + v1 파일 호환(없으면 빈 집합) + 복원 시 BSSID가 비었으면 토스트로 경고.
- **그 외 수정한 실제 결함**:
  - `while(true)` 폴링 루프에 try/catch가 없어 DataStore IOException·SQLiteException 한 번이면 프로세스 사망 → 틱 단위 예외 격리(`CancellationException`은 재throw).
  - 과거의 미마감 세션이 `nowMillis`까지 계속 누적 → 일주일 전 열린 세션이 **8640분(144시간)** 으로 표시(차트 막대는 1시간). 세션을 자기 날짜의 끝으로 캡. JDK 독립 검증으로 수정 전/후 대조 확인.
  - ARRIVE 두 개 연속 시 앞 세션을 **버리고** 덮어써서, 미래 날짜 기록 하나 추가하면 오늘 근무시간이 0분이 됨 → 앞 세션을 닫고 넘어가도록 수정.
  - `weeklyWorkedMinutes`가 상한 없이 `>= weekStart`라 미래 날짜 기록이 이번주 합계만 부풀림(차트엔 안 보임) → 양끝 경계로 제한.
  - `importBackup`이 `isAtWork` 등 세션 상태를 그대로 둬서, 복원 후 서비스가 "이미 출근함"으로 판단해 **그날 출근이 영영 기록 안 됨** → `clearSessionState()` 신설·호출, 복원 전 서비스 정지, `deleteAll`+`insertAll`을 `@Transaction`으로 묶음(중간 실패 시 전 이력 소실 방지).
  - `blankEventTemplate`이 컴포지션에서 매번 재평가되어 새 객체를 만들고, `EditEventDialog`가 그걸 `remember(event)` 키로 써서 — **입력 중 백그라운드 이벤트가 기록되면 사용자가 고른 유형/날짜/시각이 조용히 초기화**됨 → `remember`로 고정.
  - 백업 읽기/쓰기가 메인스레드(SAF URI는 클라우드일 수 있음) → `Dispatchers.IO`. 전체 이력 재계산이 매분 메인스레드 → `flowOn(Dispatchers.Default)`.
  - POST_NOTIFICATIONS를 위치 권한 요청에 얹어서만 요청 → 위치를 다른 경로로 허용한 사용자는 영영 안 물어봐서 **모든 알림이 조용히 무시됨** → 독립 요청으로 분리.
  - 배터리 최적화 다이얼로그가 설정에서 뒤로 나올 때마다 재노출(home 목적지 컴포지션이 매번 재생성) → 프로세스 스코프 플래그로 1회 제한 + `ActivityNotFoundException` 방어(해당 인텐트 없는 ROM에서 첫 실행 크래시).
  - README가 실제 동작과 어긋난 서술 3곳(자리비움을 "길이 무관 항상 기록"이라고 설명, 기준 미만도 기록되는 것처럼 읽힘, 22:00 상한이 차트에만 적용되는 점 누락) 정정 + 2연속 미스 디바운스 설명 추가.
- **10라운드 구성**: R1~4 워커 병렬 정적 리뷰 / R5 clean 회귀 빌드(40/40) / R6 컴파일 경고 전수(0건) / R7 매니페스트·권한·Gradle 검증 / R8 WorkStats 산술 JDK 독립 검증(정상 3 + 수정된 버그 6, 총 9케이스 전부 기대값 일치) / R9 실기기 배포 + **백그라운드 90초 체류 크래시 재현 시도(무크래시, 서비스·프로세스 생존)** / R10 릴리즈 서명 빌드(52/52, TRide 인증서 `3c57b1f2...41ea` 유지).
- **보고만 하고 안 고친 것(사용자 판단 필요)**: ⓐ**Doze 중 폴링 정지** — 포그라운드 서비스는 프로세스만 살릴 뿐 CPU 절전을 막지 못해 `delay()`가 안 깨어남. 야간/주말 기록 누락의 진짜 원인일 가능성이 높지만, 고치려면 폴링을 `AlarmManager`(`setAndAllowWhileIdle`) 기반으로 재설계해야 해서 코어 구조 변경 + 하룻밤 관찰 검증이 필요 → 별도 승인 후 진행. ⓑ자정 넘는 야간 근무가 07:00 클램프와 충돌해 새벽 근무분이 통째로 누락(정책 결정 사안). ⓒ`allowBackup=true`+백업 규칙 부재로 기기 이전 시 남의 세션 상태가 복원될 수 있음. ⓓ프로세스 사망 시 `rememberSaveable` 부재로 화면 상태 소실. ⓔ`EditEventDialog`의 ARRIVE/LEAVE 순서 검증 부재(퇴근<출근 저장 시 그날 0분).
- **How to apply**: (1) 세 워커가 독립적으로 같은 결론을 낸 항목(FGS 백그라운드 시작)은 신뢰도가 매우 높았음 — 영역을 겹치지 않게 나눠도 진짜 결함은 여러 각도에서 재발견된다는 신호. (2) "권한만 있으면 된다"는 가정이 Android 14+에서 깨지는 대표 사례를 확인 — 플랫폼 제약은 권한과 **프로세스 상태**가 별개 게이트일 수 있음. (3) `BackupSettings`처럼 "설정 목록을 복제한 자료구조"는 원본에 항목이 추가될 때 조용히 뒤처짐 — 새 설정을 추가할 때마다 백업 왕복을 함께 확인할 것.

## 2026-07-20: Doze 대응 알람 폴링 도입 + 가로모드 의혹 조사(앱 무관 확정)

### 1) Doze 중 폴링 정지 → AlarmManager 백스톱 추가 (사용자 승인 후 진행)
- **문제**: 포그라운드 서비스는 *프로세스*를 살릴 뿐 기기의 CPU 절전을 막지 못함. 화면 끄고 방치하면(매일 밤·주말) `delay(60_000)`이 아예 안 깨어나 감지가 침묵. 그동안 "삼성이 서비스를 죽였다"고 해석하고 배터리 최적화 예외로 대응했던 야간 기록 누락의 **상당 부분이 실은 이것**일 가능성이 높음 — 배터리 예외는 *kill*을 막을 뿐 CPU suspend와는 무관.
- **구현**: `WifiMonitorService`에 알람 체인 추가.
  - `scheduleNextPollAlarm()` — `AlarmManager.setAndAllowWhileIdle(RTC_WAKEUP, now+60s, pollPendingIntent)`. `onStartCommand`가 불릴 때마다 재무장해 체인이 끊기지 않게 함.
  - `pollPendingIntent` = `PendingIntent.getForegroundService(...)` + `ACTION_POLL` 액션 + `FLAG_IMMUTABLE`.
  - `onStartCommand`에서 `intent.action == ACTION_POLL`이면 `runOneShotPoll()` 실행(기존 60초 루프는 `monitorJob` 활성 시 재실행 안 되므로, 알람 전달만으로는 검사가 안 돌아가는 문제를 이 분기로 해결).
  - `runOneShotPoll()`은 `PARTIAL_WAKE_LOCK`(30초 타임아웃, `setReferenceCounted(false)`)을 잡고 검사 후 `finally`에서 해제 — 알람은 "전달 순간"만 깨어 있음을 보장하므로, 검사·DB 쓰기 도중 다시 잠드는 것을 막아야 함.
  - `onDestroy`에서 알람 취소 — 안 하면 사용자가 모니터링을 껐는데도 알람이 계속 기기를 깨우고 서비스를 되살림.
  - 매니페스트에 `WAKE_LOCK`(normal permission, 런타임 프롬프트 없음) 추가.
- **왜 `setAndAllowWhileIdle`인가**: `setExactAndAllowWhileIdle`은 Android 12+에서 `SCHEDULE_EXACT_ALARM`/`USE_EXACT_ALARM` 권한이 필요(민감 권한, 스토어 정책 이슈). 비정확 버전은 권한 불필요하고 Doze 중 약 9~15분 간격으로 전달되는데, 이는 자리비움 인정 기준(최소 10분, 현재 30분)보다 촘촘하므로 정확도에 실질적 영향 없음. 또한 `AllowWhileIdle` 알람 전달 시 **짧은 FGS 시작 허용 창**이 부여되어, 평소 금지되는 백그라운드 FGS 시작이 이 경로에서는 합법.
- **아직 검증 못 함**: 실제로 밤새 Doze 상태에서 알람이 깨어나 기록하는지는 하룻밤 관찰이 필요. 다음 접속 시 `adb shell dumpsys alarm | grep commute`로 알람 등록 여부, 그리고 다음 날 아침 기록 존재 여부로 확인할 것.

### 2) "앱 설치하면 가로모드가 켜진다" — 앱과 무관함이 코드 레벨에서 확정
- **조사 방법**: 소스만 본 게 아니라 **AGP가 라이브러리까지 병합한 최종 매니페스트**(`app/build/intermediates/merged_manifest/debug/.../AndroidManifest.xml`)를 직접 열어 권한 전수 확인 — 라이브러리가 몰래 주입한 권한까지 잡기 위함.
- **결정적 증거**:
  - 병합 매니페스트의 권한은 총 9개(ACCESS_FINE_LOCATION, ACCESS_NETWORK_STATE, ACCESS_WIFI_STATE, CHANGE_WIFI_STATE, FOREGROUND_SERVICE, FOREGROUND_SERVICE_LOCATION, POST_NOTIFICATIONS, RECEIVE_BOOT_COMPLETED, REQUEST_IGNORE_BATTERY_OPTIMIZATIONS)이며 **`WRITE_SETTINGS`가 없음**. 이 권한(+AppOps 특별 승인) 없이는 `Settings.System.ACCELEROMETER_ROTATION`(시스템 자동회전 토글)을 **기술적으로 변경 불가**.
  - 병합 매니페스트의 방향 관련 속성은 `screenOrientation="portrait"` 단 하나 — 이 앱 자신을 세로로 고정할 뿐 시스템 설정은 안 건드림.
  - 소스 전체(`app/src/main` 전 디렉터리) grep 결과 `accelerometer`/`user_rotation`/`WRITE_SETTINGS`/`Settings.System`/`setRequestedOrientation`/`SCREEN_ORIENTATION`/`OrientationEventListener`/`rotation` **전부 0건**.
- **결론**: 이 앱은 자동회전 토글을 켤 수 없음. 다른 원인(삼성 Routines/Bixby 자동화, 빠른설정 오터치, `WRITE_SETTINGS`를 가진 자동화 앱, 원UI 업데이트 시 설정 초기화 등)을 봐야 함.
- **다음에 기기 연결되면 확인할 것**: `adb shell settings get system accelerometer_rotation`(현재 값), `adb shell dumpsys package | grep -B5 WRITE_SETTINGS`로 그 권한을 실제로 보유한 앱 목록 확인 — 범인 후보를 좁힐 수 있음.
- **How to apply**: "이 앱이 시스템 설정을 바꾸는 것 같다"류 의혹은 소스 grep만으로 끝내지 말고 **병합 매니페스트의 권한 목록**으로 답할 것 — 권한이 없으면 그 동작은 가능성이 아니라 불가능이며, 이게 가장 반박 불가능한 증거.

### 3) 위 두 건 실기기 검증 완료 (2026-07-20)
- **Doze 알람 검증 성공**: `dumpsys alarm`에 `tag=*walarm*:com.commute.app.action.POLL` / `type=RTC_WAKEUP` / `flags=0x8`(FLAG_ALLOW_WHILE_IDLE) / `operation=PendingIntent{... startForegroundService}`로 정확히 등록된 것 확인. **화면을 끈 채 75초 대기** 후 재확인 → 발화 이력(`rtc=10:55:24`)이 남고 다음 알람(`origWhen=10:56:24`)으로 자동 재무장된 것 확인 = 체인이 실제로 돈다. 크래시 없음, 서비스 생존. (비정확 알람이라 45초 window slop이 있어 10:54:39 예정분이 10:55:24에 전달됨 — 정상.) 다만 **깊은 Doze(수 시간 방치)에서의 9~15분 간격 동작은 하룻밤 관찰이 필요**하므로 여전히 미검증으로 남김.
- **가로모드 범인 조사**: 현재 기기의 `settings get system accelerometer_rotation` = **1(켜짐)** — 사용자 관찰이 사실임을 확인(`user_rotation`=0). 서드파티 앱 전수 스캔 결과 `WRITE_SETTINGS`를 선언한 앱이 **28개**(삼성 브라우저/캘린더/뮤직/음성녹음/시계/노트/헬스/페이/원커넥트, 토스, KB국민은행, 티맵, 알리익스프레스, VLC, OneDrive, TalkBack 등)이며 **`com.commute.app`은 목록에 없음**. 앱 무관이 병합 매니페스트(권한 부재)에 이어 기기 실측으로도 재확인됨.
- **How to apply**: 사용자가 "이 앱이 시스템 설정을 바꾼 것 같다"고 의심할 때 가장 설득력 있는 답변 순서 = ①병합 매니페스트에 해당 권한 없음(불가능함을 증명) → ②기기에서 그 권한을 실제 보유한 앱 목록을 뽑아 대안 후보 제시. 단순히 "우리 코드엔 없다"보다 훨씬 강한 증거.

### 4) 자리비움 → 퇴근 자동 전이 구현 (2026-07-20)
- **사용자 제안(그대로 채택)**: 자리비움이 길어지면 그 **자리비움 시작 시각을 퇴근 시각으로** 삼아 마감한다. 기준은 ①자리비움 3시간 지속 또는 ②22시(근무 인정 시간 종료) 경과 — 둘 중 먼저 오는 쪽. 기준에 닿기 전에 회사 와이파이가 다시 잡히면 종전대로 자리비움(AWAY)으로 끝나고 세션 계속.
- **왜 중요한가**: 기존 코드에는 이 전이가 아예 없어서, 정상적인 퇴근이 기록되는 유일한 경로가 **날짜 변경 안전망**뿐이었다. 이게 "퇴근 기록 누락"의 구조적 원인. 이제 안전망은 말 그대로 예외 상황(모니터링 끔/폰 꺼짐/서비스 킬)만 담당.
- **구현**:
  - `SettingsRepository`: `autoLeaveAfterAwayMinutes`(기본 180), `workEndMinute`(기본 22*60) 신설 + 백업(`BackupData`)에도 포함(기존 v1 백업은 `optInt` 기본값으로 흡수).
  - `WifiMonitorService.checkWifiState`의 미감지 분기에서, 자리비움 승격 직후 `autoLeaveDue()`가 참이면 `setIsAtWork(false)` → 상태 클리어 → `recordEvent(LEAVE, ..., awaySince)` 순서로 마감(상태 먼저·insert 나중은 날짜변경 안전망과 동일한 이유 — 중간 취소 시 매 폴링마다 동일 LEAVE 재삽입 방지).
  - `autoLeaveDue()`는 **top-level 함수로 분리** — Service 인스턴스 없이 JVM 단위 테스트에서 부를 수 있게 하려는 목적. `AutoLeaveDueTest` 6케이스 통과.
- **핵심 설계 판단**:
  - 경과 시간을 "직전 폴링"이 아니라 **`awaySince` 기준**으로 잼 → 도즈로 폴링이 통째로 밀려도 첫 깨어난 폴링에서 올바르게 판정됨(테스트로 고정).
  - 22시 규칙은 `timestampAtMinuteOfDay(awaySince, workEndMinute)` — 자리비움이 *시작된 날*의 22시. 이미 22시를 넘겨 시작된 자리비움은 즉시 확정되는데, 이는 의도된 동작(그 시각 이후는 어차피 근무 인정 안 됨).
  - **하루 ARRIVE 1회 불변식을 의도적으로 깼다.** 자동 퇴근 후 다시 감지되면 새 ARRIVE가 기록된다 — 18시 퇴근 후 22시반 복귀는 실제로 두 번 출근한 것이고, `computeDailyWorkStats`는 원래 하루치 세션을 합산하므로 그대로 맞는다. 클래스 KDoc의 불변식 서술을 이에 맞게 새로 씀.
- **검증 한계(정직하게)**: 단위 테스트 6건 + 빌드/설치/서비스 생존은 확인했으나, **실기기에서 시간 경과로 자동 퇴근이 실제 기록되는 것은 미검증** — 폰이 보안 잠금 상태라 UI 진입 불가였고, 회사 와이파이 밖이라 세션을 만들 수도 없었음. 다음 출근일에 자연스럽게 검증됨.
- **How to apply**: 이 앱의 상태 기계를 만질 때는 "어떤 경로로 LEAVE가 기록되는가"를 먼저 물을 것. 정상 경로가 없고 안전망만 있으면 그건 기능이 아니라 버그다.

## 2026-07-20: 자동회전이 켜지는 진짜 원인 규명 — 앱이 아니라 내(Claude) `adb monkey` 검증 절차였음

- **경위**: 사용자가 두 번에 걸쳐 "이 앱만 설치·실행했는데 세로 고정이 풀리고 자동회전이 켜졌다"고 보고. 1차 때 나는 매니페스트만 확인하고 "`WRITE_SETTINGS` 권한이 없으니 기술적으로 불가능"이라며 앱 무죄로 결론냈음 — **이 결론 자체는 맞았지만, 사용자가 실제로 겪은 현상을 설명하지 못했으므로 조사로서는 실패였음.** 2차 재보고 때 추론을 버리고 기기에서 통제 실험을 했음.
- **측정 결과** (SM-S938N / Galaxy S25 Ultra, Android 16 / API 36, 앱 targetSdk 36):
  - `monkey -p <pkg> -c android.intent.category.LAUNCHER 1` 실행 → `settings system accelerometer_rotation`이 **0 → 1**로 바뀜. **패키지 3개(com.commute.app, com.android.settings, 계산기) 전부 3/3 재현.** 우리 앱과 무관한 앱에서도 동일하게 발생 → 범인은 monkey.
  - `am start -n com.commute.app/.MainActivity`로 같은 앱 실행 + `WifiMonitorService` 기동 확인 후 **60초 방치 → 계속 0** (변화 없음). 앱 무죄 확정.
  - 앱의 `WRITE_SETTINGS` appop = "No operations, default mode", 보유 런타임 권한은 알림/위치뿐. 병합·패키징된 최종 매니페스트에도 `WRITE_SETTINGS` 없음.
- **결론**: 사용자가 관찰한 인과관계("앱 설치·실행 → 자동회전 켜짐")는 **실재했지만 원인은 앱이 아니라 나**였음. 이 폰에서 앱을 띄울 때 `monkey`를 표준 절차로 써 왔고(세션 메모리에도 그렇게 적어둠), monkey가 매번 자동회전을 켜고 복원하지 않았음. 사용자 입장에선 앱 설치와 구별할 수 없었음.
- **조치**: 실험 후 `accelerometer_rotation`을 사용자의 원래 값인 0(세로 잠금)으로 복원함. 전역 메모리 `reference_adb_gitbash_gotchas.md`의 "am start 대신 monkey를 써라" 항목에 이 부작용과 복원 의무를 추가. 아울러 그 메모의 "am start가 이 폰에서 no-op된다"는 관찰도 이번엔 재현되지 않음(정상 동작) — 간헐적 현상으로 정정.
- **How to apply**: (1) 사용자가 같은 현상을 두 번 보고하면 첫 결론이 틀렸다고 보고 추론이 아니라 측정으로 다시 시작할 것. "권한이 없으니 불가능"은 앱 무죄는 증명해도 사용자가 본 현상은 설명하지 못하므로 조사 종료 사유가 못 됨. (2) 원인 후보를 앱 안에서만 찾지 말 것 — **내 검증 도구 자체가 기기 상태를 바꾼다.** 앱 vs 도구를 가르려면 "우리 앱과 무관한 다른 앱에 같은 도구를 써보는" 대조군이 결정적이었음. (3) 기기 전역 설정을 바꾸는 검증을 했으면 반드시 원복하고 사용자에게 알릴 것.

## 2026-07-20: 08:23 도착이 08:30:03으로 기록된 건 — 반올림 아님, 알람 지연

- **경위**: 사용자가 "8:23경 회사 도착했는데 첫 기록이 8:30"이라고 보고. 반올림/플렉스 슬롯 스냅 의혹부터 확인.
- **코드 조사 결론**: 이 코드베이스에 **반올림·버킷팅·근무시작시각 스냅 로직은 존재하지 않음.** `truncatedTo`/`ChronoUnit`/`/30*30` 전무. `SettingsRepository`에 `workEndMinute`은 있어도 `workStartMinute` 자체가 없음. 유일한 시각 변형은 `WorkStats.kt:83-85`의 07:00 `maxOf` 클램프인데, 07:00보다 이른 출근만 앞당기므로 08:23과 무관.
- **결정적 증거 — 기기 DB 원본**(`adb exec-out run-as ... cat databases/commute.db` → PC에서 python sqlite3로 열람. 폰에 sqlite3 바이너리 없음):
  - `[17, ARRIVE, iptime5G, 2026-07-20 08:30:03 (1784503803856)]` — **초 단위가 03, 밀리초까지 살아있음.** 반올림이면 반드시 :00.000이 됨. 즉 반올림이 아니라 폴링이 그 순간 깨어난 것.
  - 대조: 자동 감지분 `07:55:29`도 초가 남아있고, 수기 입력분은 `07:43:00.000`/`09:55:00.000`처럼 정확히 0.
- **지연의 실제 크기 측정**(`dumpsys alarm`, `logcat`):
  - POLL 알람이 화면 켜진 상태에서도 **정확히 105초 간격**으로 발화(15:28:28 → 15:30:13 → 15:31:59 …). 요청은 60초인데 `window=+44s999ms` 슬롭이 붙어 105초가 됨.
  - `policyWhenElapsed: app_standby=-1m35s` — App Standby 버킷이 추가 지연을 얹고 있음.
  - **`dumpsys deviceidle`의 Whitelist user apps가 비어 있음 → 배터리 최적화 예외 미적용.** 그래서 도즈 진입 시 `setAndAllowWhileIdle`이 9~15분 1회로 스로틀됨. 출근길(주머니 안·정지 상태)에서 딱 이 구간에 걸리면 7분 공백이 그대로 설명됨.
  - 추가 가중: `WifiUtils.kt:60,65`는 `startScan()`을 안 쓰고 OS의 **캐시된** `scanResults`를 읽으므로, AP가 실제로 보이기 시작한 시점과 앱이 인지한 시점 사이에 또 지연이 있음. `ScanResult.timestamp`는 참조하지 않음.
- **How to apply**: "기록 시각이 이상하다" 류 신고는 **먼저 DB 원본의 초·밀리초를 볼 것.** 초가 0이면 변환/반올림 버그, 0이 아니면 감지 지연이다. 이 한 번의 확인이 코드 전수조사보다 빠르게 방향을 가른다.
- **미해결(다음 작업 후보)**: (1) 배터리 최적화 예외를 사용자에게 요청(`REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`) — 지금 미적용이라 도즈 스로틀을 정통으로 맞고 있음. (2) `ScanResult.timestamp`(부팅 기준 마이크로초)로 ARRIVE를 실제 가시 시점으로 소급 보정. (3) 출퇴근 시간대만 알람 간격을 좁히기. 셋 다 미구현.

## 2026-07-20: 출근 시각 소급 보정 구현 (ScanResult.timestamp)

- **문제**: 08:23 도착이 08:30:03으로 기록. 원인은 반올림이 아니라 **폴링이 깨어난 시각을 그대로 ARRIVE로 찍은 것**(Doze/App Standby로 알람이 105초~15분 지연).
- **핵심 통찰**: 우리 폴링은 기기가 깨어 있을 때만 돌지만 **OS의 Wi-Fi 스캔은 계속 돌면서 결과마다 시각을 남긴다.** 즉 폴링이 깨어난 시점에 이미 스캔 캐시는 "AP가 몇 분 전에 보이기 시작했다"는 사실을 알고 있다. 이 차이를 회수하는 게 이번 수정.
- **구현**:
  - `WifiUtils`: `CompanyWifiDetection(nearby, observedAt)` + `detectCompanyWifi()` 신설. `isCompanyWifiNearby()`는 이제 `detectCompanyWifi().nearby`로 위임(동작 동일).
  - `ScanResult.timestamp`는 **wall-clock이 아니라 elapsedRealtime 기준 마이크로초**. `System.currentTimeMillis() - (elapsedRealtime() - timestamp/1000)`로 환산. 0/음수/부팅 시각 초과 스탬프는 드라이버가 안 채운 것으로 보고 null 처리(잘못된 스탬프로 출근을 앞당기는 게 늦는 것보다 나쁨).
  - 여러 AP가 잡히면 **최솟값**(가장 이른 관측)을 씀 — 회사 AP 중 하나라도 처음 보인 순간이 곧 도착 순간. 최신값을 쓰면 회수하려던 지연을 그대로 버리게 됨.
  - `arriveTimestamp(observedAt, now, lastEventAt)` top-level 함수(테스트 가능). 하한 2개: ①`MAX_ARRIVE_BACKDATE_MS`=30분(그보다 오래된 스캔은 "자느라 놓친 도착"이 아니라 낡은 증거) ②DB의 최신 이벤트 시각(`dao.getLast()`의 timestamp/endTimestamp 중 큰 값) — 소급이 직전 LEAVE를 앞지르면 세션이 겹쳐 `computeDailyWorkStats`가 이중 계상함. 하한이 now를 넘으면(수기 미래 기록/시계 변경) 소급 포기.
  - 라이브 연결로 매칭된 경우는 `observedAt=null` → now 사용. "연결돼 있다"는 사실은 *언제부터* 연결됐는지를 말해주지 않기 때문.
- **검증**: 단위 테스트 `ArriveTimestampTest` 6건 신규(총 13건 통과). 기기에서 `cmd wifi list-scan-results`로 **Age가 실제로 141초 등 유의미한 값으로 채워짐**을 확인(이 기기 드라이버가 timestamp를 제대로 채운다는 증거 = 수정의 전제 성립). 빌드/설치/서비스 foreground 생존/크래시 없음 확인.
- **미검증**: 실제 출근 순간의 소급이 DB에 찍히는 것은 다음 출근일에만 확인 가능. 확인법 = ARRIVE 행의 초·밀리초가 폴링 발화 시각(`dumpsys alarm` 이력)보다 앞서 있으면 소급이 동작한 것.
- **채택 안 한 후보 2개와 이유**: ①배터리 최적화 예외 요청은 `MainActivity`에 이미 구현돼 있음(사용자가 거절해 화이트리스트가 빈 상태) — 코드 문제가 아니라 사용자 선택이라 코드로 더 할 게 없음. ②출퇴근 시간대 알람 간격 좁히기는 Doze 스로틀(9~15분)이 상한이라 요청 간격을 줄여도 효과 없음.
- **How to apply**: `adb`가 PATH에 없음 → `/c/Program Files (x86)/Android/android-sdk/platform-tools`. JDK도 없음 → `JAVA_HOME="C:/Program Files/Android/openjdk/jdk-21.0.8"`. 앱 실행은 `am start -n com.commute.app/.MainActivity`(monkey 금지 — 자동회전을 켜버림, 이번에도 실행 전후 `accelerometer_rotation`=0 유지 확인함).

## 2026-07-21: 퇴근 시각 소급 보정 (출근 수정의 거울상)

- **문제**: 실제 18:55 퇴근이 18:58로 기록 = 3분 부풀림. 사용자 우선순위: **퇴근 늦게 찍히는 것(안 한 근무를 한 것처럼)만 문제.** 출근/자리비움 늦음은 허용(근무를 조금 더 계산할 뿐).
- **지연 원인 2겹**: ①스캔 캐시 잔류 — 실제로 회사를 벗어나도 OS 스캔 캐시에 AP가 한동안 남아 계속 "근처"로 읽힘(출근 땐 캐시가 우리 편, 퇴근 땐 반대로 우리를 늦춤). ②그 캐시가 비워진 뒤 **첫 미감지 폴링 시각(`awaySince`)을 퇴근으로 찍음.**
- **수정(출근 `observedAt`의 거울상 = `lastObservedAt`)**:
  - `WifiUtils`: `CompanyWifiDetection`에 `lastObservedAt`(매칭 스캔들의 **최댓값** = AP가 마지막으로 실제 관측된 시각) 추가. `observedAt`은 여전히 최솟값(출근용).
  - `WifiMonitorService`: `setLastSeenAt(now)` → `setLastSeenAt(maxOf(detection.lastObservedAt ?: now, prevSeen).coerceAtMost(now))`. 즉 lastSeenAt이 폴링 시각이 아니라 **실제 마지막 관측 시각**을 담음. prevSeen으로 바닥을 깔아 캐시 순간 역행 방지, now로 상한.
  - 자동 퇴근 LEAVE 스탬프를 `awaySince`(첫 미감지) → `lastSeenAt`(마지막 실관측)으로 변경. **판정/`autoLeaveDue`는 그대로 `awaySince`로 두고 찍는 값만 분리** — "충분히 오래 안 보였나"는 첫 미감지 기준, "언제 있었나"는 마지막 실관측 기준. lastSeenAt < awaySince 항상 성립 → 항상 더 이른(안전한) 시각.
- **불변식**: lastSeenAt은 ARRIVE 때 최초 set되므로 auto-leave 지점에서 항상 non-null(그래도 `?: awaySince` 폴백 둠). day-boundary LEAVE(라인 223)는 이미 lastSeen 사용 중 → 개선된 lastSeenAt 자동 수혜.
- **손 안 댐**: 자리비움(AWAY) 시작은 여전히 `awaySince`(첫 미감지). 사용자가 자리비움 정밀도는 문제삼지 않았고, AWAY 시작을 lastSeen으로 당기면 AWAY가 길어져 근무가 줄어드는 쪽이라 안전하긴 하나 범위 밖이라 보류.
- **검증**: `:app:compileDebugKotlin` / `compileDebugUnitTestKotlin` 클린. 실기기 실검증(실제 퇴근 시 DB LEAVE 초·밀리초가 첫 미감지 폴링보다 앞서는지)은 다음 퇴근일에만 가능 — 미완.

## 2026-07-21: BLE 재실감지 실현성 실측 (Wi-Fi 끄는 사람 대비책 조사)

- **경위**: [[project_commute_wifi_scan_detection]]는 Wi-Fi 라디오가 켜져 있어야만 작동 → Wi-Fi 끄고 LTE만 쓰는 직원은 재실 감지 불가. 사용자가 "BLE로 고정 앵커를 잡고 Wi-Fi 보조로" 방향 확정 후 "내 폰으로 BLE 잡히는지 확인" 지시.
- **측정 방법(앱 없이 adb만, 루트 없음)**: 루트 없어 `/data/misc/bluetooth/logs` 직접 읽기 불가, `cmd bluetooth_manager`엔 스캔 서브커맨드 없음. → `am start -a android.settings.BLUETOOTH_SETTINGS`로 프레임워크 LE 스캔 유발 + `logcat`에서 `ScanController: onScanResult`(address/addressType/rssi/eventType) 파싱. 폰은 위치용 상시스캔(`ble_scan_always_enabled=1`)+삼성 beaconmanager/mcfds 상시 광고 상태라 스캔은 잘 돎. 화면 잠금이어도 스택 스캔은 정상 수집됨.
- **결과(25초, 위치=폰이 USB로 이 PC에 물린 곳, 화 10:44)**: onScanResult 125건 / 고유 광고자 ~19개. **addressType 전부 =1(랜덤/회전주소), public(0) 0개.** eventType: connectable(0x1b) 104건=고유 13기기(폰/워치/이어버드), non-connectable(0x10/0x00) 비콘후보 4개(...79:19, ...80:12 rssi-59~-66 최강, ...9A:42, ...EF:BE). logcat에 페이로드 일부 누출 확인 → `makeManufacturerData: 75 00 ...`(0x0075=Samsung 제조사데이터, ASCII `[Phone]`), `[MCFServer]_McfBleAdvertiser`(=우리 폰 자신의 SmartThings Find 광고). 즉 잡힌 건 전부 소비자 기기.
- **핵심 결론**: (1) **BLE 감지 능력 자체는 매우 우수** — 앱 없이, 잠금 상태에서도 프레임워크 스캐너가 초당 수건씩 광고 수집. 우리가 비콘을 놓으면 확실히 잡힘. (2) **하지만 주변 ambient BLE는 앵커로 못 씀** — 요즘 모든 소비자 기기가 MAC 랜덤화(addressType=1)라 같은 기기도 ~15분마다 주소가 바뀜. MAC 기반 고정 앵커 불가. 고정 public 주소 기기(기업 AP의 BLE 광고, 프린터 등) 0개. (3) non-connectable 4개가 진짜 고정 iBeacon/Eddystone인지 아니면 폰의 오프라인 파인딩(SmartThings Find/Find My, 이것도 회전)인지는 **페이로드 UUID를 봐야** 확정 — logcat은 페이로드 미제공, 확정하려면 스눕로그+bugreport 필요(이번엔 안 함, 비쌈).
- **How to apply / 권고**: ambient BLE 라칭(주변 기기에 붙기)은 MAC 랜덤화 때문에 구조적으로 불안정 → BLE로 가려면 **우리가 통제하는 고정 비콘을 사무실에 설치**(싼 iBeacon 또는 항상 켜진 사무실 기기에 고정 UUID 광고)하고 그 **페이로드 UUID**로 식별하는 게 정답. MAC이 아니라 UUID로 봐야 함. 감지 신뢰성은 이미 실측으로 입증됨. 다음에 "기존 비콘이 이미 있나" 정밀 확정이 필요하면 스눕로그+bugreport로 4개 non-connectable 광고자의 페이로드를 읽을 것. 측정 정리: `settings put secure bluetooth_hci_log 0` 원복함, 홈 복귀함.

## 2026-07-21(후속): "사무실 프린터·스피커를 앵커로 못 쓰나?" 실측 — 기기 클래스가 관건

- **질문**: 사무실에 프린터·음악스피커가 있는데 그걸로 재실 감지 못 하나?
- **측정(이번엔 사무실에서. 회사 AP `58:86:94:03:a8:92` -59로 재실 확인)**:
  - `dumpsys bluetooth_manager`의 **bonded devices(6개)**: QCY AilyPods, SORENTO(차), CLOVA-CLOCK+3-FF2(네이버 클로바, DUAL), Xiaomi Smart Band 10, MEDICAL PHANTOM, S3 — **전부 Public(고정) 주소.** 대부분 BR/EDR(클래식), CLOVA만 DUAL.
  - **Other devices 캐시(최근 본 것)**: `[AV] Samsung Soundbar Q800T`(DUAL), `[TV] Samsung UA8180 85 TV`(DUAL), `JBL LIVE PRO 2 TWS-LE`, `LG HBS-PL5` 등 — **AV기기·이어버드는 Public(고정) 주소로 LE 광고함.** → **핵심 반증**: 지난번 "전부 랜덤"이었던 건 그때 주변에 사람 폰/워치/이어버드뿐이었기 때문. 스피커·사운드바·TV 클래스는 **MAC을 안 바꾼다.**
  - **그런데 지금 사무실 라이브 LE 스캔(20초)**: onScanResult 78건 **전부 addressType=1(랜덤), public 0개.** 위 사운드바/TV는 집·매장 캐시지 이 사무실 게 아님. **지금 이 사무실 반경엔 LE로 광고 중인 고정주소 기기가 없음.**
- **왜 안 잡히나(종류별 판정)**:
  - **클래식 BT 스피커**(bonded S3/SORENTO처럼 BR/EDR): passive 스캔에 **원리적으로 안 뜸.** BLE 광고를 안 쏘고, "검색모드(페어링대기)"일 때만 능동 inquiry로 잡힘. 켜져 있어도/연결돼 있어도 조용. 주소는 고정이나 외치질 않음.
  - **스마트 스피커/사운드바**(Q800T·CLOVA처럼 DUAL): LE 상시광고 → 고정주소로 잡힘 → **앵커 사용 가능.** 단 이 사무실엔 지금 범위에 없음.
  - **프린터**: Wi-Fi 프린터는 네트워크 장비 → **Wi-Fi 켠 사람만** 발견 → 대상(Wi-Fi 끄는 사람)에겐 무용. BLE 프린터는 설정 시에만 잠깐 광고.
- **결론/다음**: "기존 스피커 재활용" 가능 여부는 **그 스피커가 스마트/사운드바(DUAL·LE 상시광고)인지 클래식인지**로 갈림. 사용자에게 사무실 스피커 모델명 문의함(미회신). 스피커 켠 상태로 사무실 재스캔하면 즉판 가능. **가장 확실한 답은 여전히 우리가 통제하는 고정 iBeacon 설치**(기존 기기 의존은 "마침 LE 상시광고 종류인가" 운에 걸림).
- **측정 방법 메모**: bonded/seen 목록은 `dumpsys bluetooth_manager`에서 나옴(주소는 XX 마스킹되나 name·주소유형·전송(BR/EDR·LE·DUAL)은 보임). 라이브 스캔은 여전히 `am start -a android.settings.BLUETOOTH_SETTINGS` + `logcat | grep onScanResult`의 addressType 집계. `cmd bluetooth_manager`엔 scan 서브커맨드 없음(enable/disable/enableBle뿐).

## 2026-07-21(후속): 비콘 구매 가이드 — 10m는 최저스펙, 진짜 기준은 전원+고정주소

- **질문**: "비용 적으면서 반경 10m 이상 커버하는 비콘 사고싶다."
- **핵심 조언**: **10m 커버리지는 사실상 모든 iBeacon/Eddystone 비콘이 넘기는 최저 스펙**(저출력도 개활 10~30m, 최대 30~70m, 사무실 파티션 감안해도 방 하나 무조건 덮음). 따라서 "거리"로 기종을 고를 필요 없고, 오히려 너무 세면 복도/옆방 누출로 오검출 → **TX 파워 조절 가능한 게 더 중요.** 실제 선택 기준 2가지: ①**전원**(사무실 앵커의 유일한 실패원인은 "배터리 방치" → 콘센트/USB 있으면 상시전원 압도적 우위), ②**고정 public MAC + 설정 가능한 UUID**(우리 앱이 식별자로 필터해야 함 — 지난번 ambient가 다 랜덤주소라 못 쓴 바로 그 문제. 전용 비콘은 대부분 고정 public MAC. 이게 비콘 사는 핵심 이유).
- **추천 2갈래**: ⓐ상시전원(USB/콘센트 있음): **ESP32 개발보드를 iBeacon으로 굽기(₩5~8천)** — 제일 싸고 배터리 실패 원천 없음, MAC·UUID·파워 완전 통제(우리 앱 필터와 궁합 최상), 단 최초 1회 펌웨어 플래시 필요. ⓑ배터리(아무데나 부착): MOKO/Feasycom/Blue Charm BC011 계열(₩1.5~3만) — 앱으로 UUID/TX 설정, 부착만 하면 끝. **단 코인셀 CR2032는 피하고 CR2477 또는 AA 모델**(1~3년 수명). 최우선 추천은 콘센트 있으면 ESP32.
- **수량**: 방 하나=1개면 충분하나 앵커 죽으면 그 구역 전원이 미출근 처리되니 **예비 1개 포함 2개** 권장(저렴한 이중화 보험).
- **사용자에게 되물은 것(미회신)**: ①비콘 놓을 자리 근처 USB/콘센트 유무(있으면 ESP32 확정), ②감지 대상이 방 하나인지 여러 층/구역인지(구역 수=비콘 수). 회신 오면 쿠팡/알리 구체 구매 링크까지 좁힐 것.
- **다음 단계(구매 후)**: 앱에 BLE 스캔 경로 추가 필요 — MAC이 아니라 **페이로드 UUID로 식별**(위 [[BLE 실측 항목]] 결론). `BluetoothLeScanner.startScan(filters, settings, PendingIntent)`로 OS 레벨 백그라운드 스캔 가능(Wi-Fi 스캔 캐시 방식보다 오히려 백그라운드 지원 좋음). Wi-Fi 기본 + BLE 보조 구조로 갈 것.

## 2026-07-21: BLE 보조 앵커 하드웨어 확정 — USB 전원 가능 → ESP32 개발보드

- **경위**: Wi-Fi 끄고 LTE/5G만 쓰는 직원 재실 감지 불가 문제 → BLE 비콘 보조 앵커 도입 방향 확정(Wi-Fi 주, BLE 보). 실측(25초 스캔, 고유 ~19개)에서 사무실 광고자가 전부 랜덤(회전)주소라 기존 기기를 앵커로 못 씀 → 우리 비콘을 직접 놓는 방향. 프린터/스피커도 현재 사무실에선 안 잡힘.
- **사용자 확정**: "USB에 꽂을 것이면 될거 같아" — 비콘 놓을 자리에 USB 전원 있음.
- **결정**: **ESP32 개발보드(DevKit, USB 포트형), ₩5~8천, 2개 권장(1 예비).** USB 상시전원이라 배터리 비콘(CR2477/AA, ₩1.5~3만) 대신 ESP32가 최선.
  - 핵심 이유: ESP32는 **고정(public) MAC**으로 iBeacon/Eddystone 상시광고 가능 → 랜덤주소 문제 우회. 10m 커버리지는 과충족(오히려 출력 낮춰야 할 수 있음).
  - 구매 주의: 완제품 비콘 말고 **개발보드(DevKit) 형태**로 살 것(USB로 펌웨어 굽고 전원도 받음).
- **다음 단계(미착수)**: (1) ESP32 iBeacon 펌웨어 작성+굽기, (2) 앱에 BLE 스캔 보조신호 추가(BluetoothLeScanner.startScan + PendingIntent 백그라운드, UUID 필터). 사용자에게 (A)구매링크 좁히기 vs (B)펌웨어 코드 준비 중 선택 대기 중.

## 2026-07-21(후속): ESP32 개발보드 → USB 완제품 비콘으로 방향 선회 제안

- **경위**: 사용자가 "ESP32는 집에 있는 것 같은데 찾기 어렵고, 개발보드 말고 USB타입 간단한 완제품은 없나?"라고 물음. 즉 펌웨어 플래싱/납땜 없이 **꽂기만 하면 되는 완제품**을 원함.
- **답변한 대안**: **플러그앤플레이 USB 완제품 비콘** — USB 메모리처럼 생겨 5V USB 충전기에 꽂으면 앱 설정 한 번으로 고정 public MAC + iBeacon/Eddystone 24시간 광고. ESP32의 장점(상시전원·고정주소)을 유지하면서 코딩/플래싱만 제거한 버전. 상시전원(콘센트 넉넉) 확정 상황에 최적.
  - **구체 제품 3종**(웹검색 확인): ⓐ**MOKO L05**(L05I 내장안테나/L05E 외장) — 대량·안정 배치용 설계, 가장 무난 추천. ⓑ**KKM USB Beacon U1** — 앱/게이트웨이로 전 파라미터 수정. ⓒ**Feasycom FSC-BP109**(Qualcomm CSR8811) — 단 최대 4000m급 고출력이라 **TX 파워 낮춰 배치 필수**.
  - **공통 주의(앱 로직과 직결)**: 완제품은 기본 송신출력이 세서 그대로 두면 복도/옆방까지 잡혀 재실 오검출 → 구매 후 앱에서 **TX Power를 낮게(-12~0 dBm 근처)** 조정해 사무실 반경만 커버. 우리 앱의 RSSI 임계값 튜닝과 짝.
- **되물은 것(미회신)**: ①구매처(쿠팡/네이버 국내 빠른배송 vs 알리 직구 개당 1만원대), ②개수(방 하나면 2개=주1+예비1, 콘센트 여러 곳이면 구역별 추가). 회신 오면 구체 구매 링크 좁히고 그다음 앱 BLE 앵커 감지 로직 착수.
- **참고**: ESP32(₩5~8천)가 여전히 최저가지만, 사용자가 플래싱 부담·기기 분실로 완제품 쪽으로 기운 상태. 둘 다 고정 public MAC이라 앱 필터 관점에선 동등. [[BLE 실측 항목]]대로 식별은 MAC이 아니라 페이로드 UUID로 할 것.

## 2026-07-22: 노트북 PC를 BLE 비콘으로 쓰는 A작업 실측 — iptime BT50(Realtek)은 "재부팅 필요" 상태로 활성화 불가, 광고 SW는 C#로 성공

- **사용자 지시**: "A작업을 해서 확인해보자. 이 컴이 노트북인데 노트북 내장 BT도 있고 USB동글도 있는데 USB동글 BT50으로 확인해보자." — PC(노트북)를 BLE 광고(비콘)로 만들어 폰이 잡는지, 특히 **iptime BT50 USB 동글**로 검증하려는 것.
- **하드웨어 실측(확정)**: 이 노트북엔 BT 라디오 2개 존재 — ①**Intel 내장**(`USB\VID_8087&PID_0033`, 현재 활성), ②**Realtek Bluetooth 5.0 Adapter**(`USB\VID_0BDA&PID_8771`) = **iptime BT50 동글의 정체**(BT50은 Realtek RTL8761B 칩). 현재 활성 라디오는 Intel임(BLE 열거자 `BTH\MS_BTHLE`의 부모가 VID_8087로 확인). QCY AilyPods 이어폰이 Intel에 붙어 동작 중.
- **핵심 제약(Windows 구조적)**: Windows BT 스택은 **한 번에 라디오 1개만** 시스템 기본으로 씀. BT50은 설치돼 있으나 **`CM_PROB_NEED_RESTART`(코드14, "재시작해야 동작")** 상태로 막혀 있었음(맨 처음 `Get-PnpDevice -Status OK` 목록에 BT50이 안 나온 이유). 즉 동글을 꽂아도 재부팅 전엔 활성 라디오가 안 됨.
- **시도한 것과 결과**:
  - `Disable-PnpDevice`로 Intel 끄기 → **CIM 일반오류(0x80041001)로 실패**(반복 실패). `pnputil /disable-device`도 실패: **"Device is pending system reboot to complete a previous operation" + "This command is not supported on this OS product"**. → 이미 재부팅 대기 상태라 장치 토글이 적용 안 됨.
  - Realtek을 disable→enable로 "장치 재시작" 시도 → `CM_PROB_NEED_RESTART`가 **`CM_PROB_FAILED_ADD`로 악화**(Intel이 라디오 슬롯을 점유 중이라 Realtek이 못 올라옴).
- **광고 소프트웨어(WinRT) — PS 5.1은 불가, C#는 성공**:
  - PowerShell 5.1은 WinRT `BluetoothLEAdvertisementPublisher`를 못 구동함 — 제조사데이터의 `IBuffer` 대입은 `.AsBuffer()`로 우회되지만 **컬렉션 `ManufacturerData.Add()`/`ServiceUuids.Add()`(IVector 투영)가 근본적으로 안 됨**. (`DetachBuffer`/`CryptographicBuffer`는 ComObject→IBuffer 변환 실패)
  - **해결**: C#로 광고기 컴파일. `csc.exe`(v4.0.30319) + 참조 `UnionMetadata\10.0.26100.0\Windows.winmd` + `System.Runtime.WindowsRuntime.dll` + `.NETFramework\v4.8.1\Facades\System.Runtime.dll`(+`...InteropServices.WindowsRuntime.dll`). 소스: `C:\Users\lab\AppData\Local\Temp\pcbeacon\beacon.cs`, 산출물 `beacon.exe`(CompanyId 0x0059, payload "COMMUTE", 인자=광고 초). **컴파일 성공.**
  - **단, 실행 시 `StatusChanged -> Aborted (error=RadioNotAvailable)`** — 광고가 붙을 라디오가 사용 불가. 이어폰(클래식 BT A2DP/AVRCP)은 여전히 OK인데 BLE 광고만 죽음 → 위 **pending-reboot + 장치 토글로 LE 광고 스택만 어긋난 상태**로 진단.
- **결론(사용자에게 보고)**: (1) BT50으로 실제 비콘 테스트하려면 **재부팅 필수** — 우회 스크립트 없음. (2) 재부팅만으로는 여전히 Intel이 기본 라디오라, BT50을 쓰려면 재부팅 전에 Intel 라디오를 비활성(장치 관리자/설정에서) 예약하거나, 재부팅 후 BT50을 기본으로 승격해야 함. (3) 광고 SW(`beacon.exe`)는 이미 동작 가능 — 스택만 깨끗하면 바로 Started 됨. (4) 폰은 지금 adb에 안 붙어 있음(수신 검증하려면 폰 USB 연결 또는 nRF Connect 앱 필요). adb 위치: `C:\Program Files (x86)\Android\android-sdk\platform-tools\adb.exe`.
- **How to apply**: PC를 BLE 비콘으로 쓰는 건 기술적으로 되지만 **노트북 2-라디오 환경에선 재부팅/라디오 전환 관리가 번거로움** — [[ESP32/USB 완제품 비콘 항목]]의 "꽂으면 끝" 완제품이 여전히 더 실용적. PC 비콘은 어디까지나 하드웨어 도착 전 임시 검증용. 다음에 이 검증을 이어가려면: 재부팅 → (Intel 비활성 원하면 먼저 예약) → `beacon.exe N` 실행해 Started 확인 → 폰 nRF Connect로 CompanyId 0x0059 "COMMUTE" 광고 수신 확인.

## 2026-07-22: 퇴근 시각이 실제보다 3~4분 늦게 잡힌다 — 버그 아님(Wi-Fi 전파 꼬리), 고정 마진 비추

- **사용자 관찰**: "어제 퇴근 시각이 19:18로 기록됐는데 실제 퇴근은 19:14경, 15분경 반경을 벗어났다. 퇴근이 더 늦게 잡힌 거 같은데 마진을 두고 퇴근 처리를 해야 할까?"
- **원인(코드 확인)**: 퇴근 시각은 `WifiMonitorService`가 `lastSeenAt`(= 회사 AP를 마지막으로 **실제 감지한 스캔**의 시각)으로 찍음. `WifiUtils.wallClockSeenAt()`가 스캔 자체의 elapsed-realtime 타임스탬프로 계산하므로, 낡은 캐시가 늦게 찍히는 게 **아님** — 19:18은 폰 Wi-Fi 스캔이 사무실 AP 비콘을 마지막으로 진짜 감지한 시각이 맞음. "스캔에 뜬다"(약 -90dBm이면 감지)는 "연결 가능"보다 훨씬 낮은 문턱이라, 체감상 방을 나선 뒤(19:14)에도 복도·엘리베이터 홀·계단까지 벽 너머로 3~4분 더 비콘이 잡힘. **RF 근접감지에 내재된 "꼬리(tail)"**이고, 방향은 항상 "약간 늦게 = 근무시간 소폭 과대인정" 쪽으로 systematic.
- **왜 고정 마진(N분 빼기)은 비추라고 판단**: (1) 꼬리 길이가 건물구조·AP출력·동선에 따라 매번 다름(1~5분) → 일괄로 빼면 짧은 꼬리인 날엔 오히려 실제 근무한 시간을 깎음(under-credit). (2) 출근 backdate 로직(`arriveTimestamp`)의 철학은 "확실하지 않으면 근무시간을 지어내지 않는다"인데, 마진은 반대로 "확실한 근무시간을 임의로 깎음" → 리스크 방향이 반대편으로 생김. (3) 마진 넣으려면 출근 backdate처럼 바닥(출근 시각·직전 이벤트보다 앞서면 안 됨)을 또 깔아야 해서 단순하지도 않음.
- **권고(사용자에게 보고)**: 지금은 그대로 두는 게 나음 — 4분은 RF 고유 불확실성 안이고 방향도 컴플라이언스상 치명적이지 않으며, 기록 누락 배너로 수동 보정도 가능. 만약 이 꼬리가 반복적으로 거슬리면 **고정 마진 대신 RSSI(신호세기) 바닥값 방식**이 정답 — 스캔 결과의 `level`(RSSI)을 이용해 "**강하게** 잡힌 마지막 스캔"만 퇴근 시각으로 인정하면 방 밖의 희미한 꼬리가 자동으로 빠짐. 단 임계값 튜닝을 위해 이 사무실의 "안/밖" RSSI 경계를 한 번 실측해야 함. **사용자 답변 대기 중**(그냥 둘지 / RSSI 방식으로 갈지).
- **How to apply**: 이 앱의 퇴근 시각은 이미 `lastSeenAt` backdate로 "늦게 알아챈 것"은 보정됨 — 남은 오차는 순수 RF 꼬리라 시간 기반 보정(마진)이 아니라 신호세기 기반 게이팅으로 접근할 것. `detectCompanyWifi`는 지금 `level`을 안 읽고 stamp만 뽑으니, RSSI 방식을 하려면 `ScanResult.level`을 `CompanyWifiDetection`까지 실어 나르는 배관부터 필요.

---

## [2026-07-22] 노트북을 BLE 비콘으로: 내장 Intel BT는 광고(advertise) 불가 — Windows 실측 확정

- **배경**: BLE 앵커를 "사무실에 놓을 비콘"으로 마련하려는 흐름. 사무실엔 상시광고 중인 고정주소 기기가 0개(폰 스캔 실측)라 우리 비콘을 놓는 게 정답. 노트북이 상시 가동이라, 별도 하드웨어 없이 **노트북 내장 BT를 BLE 광고 주체로 쓸 수 있는지**를 검증함(작업 A). USB 동글 iptime BT50(Realtek RTL8761B)은 내장 Intel이 시스템 주 라디오라 "Error"로 밀려 못 씀 → 사용자가 동글 빼고 내장으로 확인 요청.
- **실측 환경**: 활성 라디오 = Intel 내장(`USB\VID_8087&PID_0033`), QCY 이어폰 클래식 연결 중. Bluetooth 라디오 전원 State=On.
- **능력 조회**: `BluetoothAdapter.GetDefaultAsync()` → `IsLowEnergySupported=True`, `IsPeripheralRoleSupported=True`, 어댑터 public MAC=`00A5547CA3F7`.
- **핵심 결과(반전)**: `IsPeripheralRoleSupported=True`인데도 실제 `BluetoothLEAdvertisementPublisher.Start()`가 **Waiting → Aborted, Error=RadioNotAvailable**로 3회 연속 실패. 전원 문제 아님(라디오 On). → **Intel 내장 BT는 API로는 peripheral 지원한다고 보고하지만 Windows/Intel 드라이버가 실제 LE advertising을 거부하는 알려진 한계.** 슬롯 초과가 아니라 드라이버가 기능을 안 엶.
- **곁가지 확정 사실(PowerShell 5.1 한계, 블투와 무관)**: PS 5.1은 WinRT `IBuffer`(제조사데이터), `IVector<Guid>`(ServiceUuids) 프로젝션을 못 해서 `System.__ComObject` 변환 에러. `LocalName`은 문자열이라 세팅은 되지만 **Windows 퍼블리셔가 LocalName/Flags를 직접 관리해 커스텀 LocalName은 Start에서 ArgumentException("Value does not fall within the expected range")로 거부**. → PS로는 광고 실증 불가, **dotnet(net10.0-windows10.0.19041.0) 콘솔로 CsWinRT 프로젝션 써서 검증해야 함**. 코드/프로젝트는 `doc/ble-beacon/`(Program.cs, beacon.csproj)에 있음.
- **31바이트 광고한도 실측**: 128비트 ServiceUUID(18B)+제조사데이터(12B)+Flags(3B)=33B > 31 → Start에서 ArgumentException. 앵커는 **둘 중 하나만** 실어야 함. 제조사데이터 단독(company 0xFFFF + "COMMUTE1" = 12B)은 콘텐츠 승인됨(그 다음 라디오 단계에서 RadioNotAvailable로 죽은 것).
- **다음 선택지(사용자 보고, 결정 대기)**: (A) **iptime BT50 동글로 재시도** — Realtek 계열은 보통 광고를 실제 지원함. 단 Windows가 라디오 1개만 쓰므로 장치관리자에서 **내장 Intel BT를 비활성화**해 시스템이 동글로 폴백하게 해야 하고, 그 순간 QCY 이어폰 연결이 끊김(부작용). (B) **ESP32/USB 완제품 비콘(MOKO L05 등)** 원안 복귀 — Windows 광고 제약을 통째로 우회, "꽂으면 끝". 노트북 상시가동이라 (A)가 되면 추가 하드웨어 0원이지만, Intel 비활성화·이어폰 끊김·동글 상시점유 트레이드오프 있음.
- **How to apply**: 노트북 내장 BT를 비콘으로 쓰는 안은 이 하드웨어(Intel)에선 **폐기**. 앵커가 필요하면 (A) 동글 실측(내장 비활성화 필요) 또는 (B) 전용 USB 비콘. 앱 쪽 필터는 MAC 회전을 견디도록 **제조사데이터 페이로드/ServiceUUID 기반**으로 설계할 것(고정 MAC 앵커 금지 — Windows/폰 둘 다 랜덤주소).

## [2026-07-22] 작업 A(BT50 동글) 착수 — 내장 Intel 비활성화가 재부팅에 막힘, 사용자 재부팅 승인 대기

- **지시**: "내장 비활성화하고 BT50으로 해보자. 안 되면 ESP32 구매."
- **실측한 라디오 인스턴스ID**: 내장 Intel = `USB\VID_8087&PID_0033\5&2BC76CDA&0&10`(활성/OK), BT50 동글(Realtek RTL8761B) = `USB\VID_0BDA&PID_8771\00E04C239987`.
- **BT50 Error의 정확한 원인 = ProblemCode 31**(드라이버 로드/시작 실패), 밀려난 게 아님. 드라이버 자체는 정상 존재(oem161.inf, Realtek BT-class 1.6.1015.3018, 스토어에 매칭 패키지 확인). `pnputil /restart-device`로 재시작해도 여전히 code 31 → **Intel이 시스템 주 라디오인 한 두 번째 라디오는 code 31로 시작 자체가 안 됨**(Windows 단일 BT 라디오 제약이 code 31로 발현). 즉 BT50을 쓰려면 Intel 비활성화가 필수.
- **Intel 비활성화가 막힌 이유**: `Disable-PnpDevice`(CIM)는 HRESULT 0x80041001 "일반 오류", `pnputil /disable-device`는 "Device is pending system reboot to complete a previous operation" + "not supported on this OS product". 확인 결과 **① 내 disable 시도가 이미 예약됨**(Intel `ConfigFlags=1`=CONFIGFLAG_DISABLED 비트 세팅 → 재부팅하면 꺼진 채로 부팅) **② Windows Update 재부팅이 원래도 대기 중**(`HKLM\...\WindowsUpdate\Auto Update\RebootRequired` 키 존재) — 이 WU 대기가 즉시-disable을 막고 있던 "previous operation".
- **다음 단계(사용자 승인 대기)**: **재부팅 1회** 필요. 재부팅하면 (a) Intel이 disabled로 올라오고 (b) BT50이 code 31에서 풀려 시스템 주 라디오가 될 것으로 예상 → 그 후 `doc/ble-beacon/`의 dotnet 콘솔로 BT50 광고 실측. WU 재부팅이 어차피 대기 중이라 재부팅은 "필요한 상태"이기도 함. 부작용: 재부팅 후 QCY 이어폰은 내장이 아니라 BT50으로 붙어야 함(BT50이 살아야). 노트북이 상시가동 업무기라 사용자 명시 승인 후에만 재부팅할 것.
- **롤백 경로**: BT50이 실패하면 Intel 재활성화 = `Enable-PnpDevice -InstanceId "USB\VID_8087&PID_0033\5&2BC76CDA&0&10"` 또는 `pnputil /enable-device`. 그 뒤 ESP32/USB 완제품 비콘(원안 B)로 전환.

## [2026-07-22 재부팅 후] 작업 A 성공 — BT50 동글이 BLE 광고 송출 확정, ESP32 구매 불필요

- **결과(실측)**: 재부팅 후 라디오 상태가 정확히 반전됨 — Realtek BT 5.0(`VID_0BDA&PID_8771`) = **OK(유일 활성)**, Intel 내장(`VID_8087`) = **Error(비활성화 반영됨)**. 예상대로 (a) Intel disabled로 부팅 (b) BT50이 code 31에서 풀림.
- **BLE 광고 실측 통과**: `RADIO: Bluetooth = On` → `STATUS=Waiting` → `STATUS=Started ERROR=Success` → `FINAL: Started`. 내장 Intel이 3회 모두 `RadioNotAvailable`로 거부하던 그 peripheral/advertise 기능을 **BT50은 정상 송출**. → **이 노트북(상시 가동 업무기)을 BLE 비콘으로 확정 채택, ESP32/USB 완제품 비콘 구매 불필요.**
- **검증 도구의 함정(중요)**: 이 광고 실측은 **Windows PowerShell 5.1로는 불가능**. PS 5.1은 WinRT 제네릭 프로젝션이 안 됨 — `BluetoothLEManufacturerData.Data`(IBuffer) 대입, `Advertisement.ManufacturerData.Add`/`ServiceUuids.Add`(IVector<T>) 모두 반환값이 raw `System.__ComObject`라 `.Add`/IList 캐스팅 전부 실패. `DataWriter.DetachBuffer()`, `CryptographicBuffer`, `AsBuffer()` 다 소용없음. **빈 광고는 Start가 `0x8007000D "data is invalid"`로 거부**되므로(제조사 데이터든 UUID든 payload가 반드시 있어야 함) 이 마샬링을 못 넘기면 테스트 자체가 불가.
- **통한 방법 = dotnet SDK 프로젝트**: `net9.0-windows10.0.19041.0` TFM로 최소 콘솔 프로젝트를 만들면 CsWinRT가 WinRT를 정식 프로젝션해줘서 위 코드가 그대로 컴파일·동작. (csc.exe 직접 컴파일은 winmd가 System.Runtime(System.Attribute)을 요구 → facade 넣으면 mscorlib와 System.Exception 중복(CS0433)으로 꼬여서 포기.) 이 머신엔 dotnet 9.0.314/10.0.300 SDK 있음. 소스는 `C:\Users\lab\AppData\Local\Temp\bletest2\`(Program.cs=Radio 상태 출력 + Publisher Start까지 검증). 이걸 `doc/ble-beacon/`로 정리하면 상시 실행용 비콘 앱의 출발점.
- **다음 단계 후보**: (1) 이 광고를 상시 실행 서비스/트레이앱으로 만들어 회사 자리에 고정된 노트북이 늘 비콘을 쏘게 함. (2) 안드로이드 앱 쪽에서 이 제조사 데이터(CompanyId 0xFFFF + "COMMUTE")를 BLE 스캔으로 재실감지 앵커로 사용(Wi-Fi 끄는 사람 대비책, [[BLE 재실감지 실현성]] 계열). (3) QCY 이어폰이 이제 BT50에 붙는지/음질 이상 없는지 사용자 체감 확인.

## [2026-07-22] 퇴근 시각 마진(RF 꼬리 보정) 설정으로 구현 — 기본 3분

- **결정**: Turn 6/10에서 "고정 마진 비추"라 했지만, 사용자가 반복적으로 거슬려 **설정 가능한 마진**으로 결정. Turn 10 권고(RSSI 바닥값 방식)는 안 감 — 훨씬 큰 작업이고, 설정형 고정 마진이 사용자 요구("자리 이탈 후 시간을 당겨준다")에 직접 부합.
- **왜 이게 예전 "비추" 논리와 안 부딪히나**: 예전 우려는 "출근 로직의 근무시간 안 지어낸다 철학과 반대(짧은 날 일한 시간 깎임)". 이번엔 **기본값 3분 + 사용자가 0분(끔)~10분 조절**로 판단을 사용자에게 넘겼고, 하한(출근 시각) 바닥을 둬서 세션이 음수가 되진 않음.
- **핵심 함수** `WifiMonitorService.kt`의 top-level `leaveTimestamp(lastSeenAt, marginMinutes, floorAt)` — `arriveTimestamp`와 같은 모양(테스트 가능). `lastSeenAt - margin`, 단 `floorAt`(=직전 기록=이 세션 출근) 밑으로는 못 감. `floorAt >= lastSeenAt`면 보정 포기. margin 0이면 그대로.
- **적용 지점 2곳**: ① 정상 자동 퇴근(auto-leave, lastSeenAt 스탬프) ② 날짜경계 자동 마감 — 둘 다 lastSeenAt 기반이라 같은 꼬리 편향이 있어 동일 헬퍼 적용. AWAY(자리비움) 종료 시각(=복귀 now)에는 **적용 안 함**(퇴근만).
- **설정 배선**: `SettingsRepository.leaveMarginMinutes`/`setLeaveMarginMinutes`/`DEFAULT_LEAVE_MARGIN_MINUTES=3`, `CommuteViewModel` StateFlow+setter, `BackupData`(BackupSettings 필드+직렬화+파싱, 구백업 기본 3분), `SettingsScreen`에 "퇴근 시각 마진" RuleCard(0~10분 드롭다운, 즉시저장 no-debounce — 고정 선택지라 미완료 상태 없음).
- **lastSeenAt 자체를 실제 마지막 스캔 시각으로 교체(중요)**: 마진 보정은 lastSeenAt이 "폴링한 시각"이 아니라 "AP를 실제로 마지막으로 본 스캔 시각"일 때만 의미가 있음 — 그래서 `WifiUtils.CompanyWifiDetection`에 `lastObservedAt`(매칭 스캔들 중 **최신** 시각, `observedAt`=최소는 출근 백데이트용) 신설, `detectCompanyWifi`가 둘 다 반환. `WifiMonitorService`의 `setLastSeenAt(now)`를 `maxOf(detection.lastObservedAt ?: now, 직전 lastSeenAt).coerceAtMost(now)`로 교체(캐시가 순간 stale해도 뒤로 안 밀리게 바닥, 미래 방지 상한, 라이브연결 매칭이면 now 폴백). 즉 마진은 이 정확해진 lastSeenAt에서 빼는 것.
- **설명문구 제거(이번 세션 재개 후 수정)**: 이전 세션이 `LeaveMarginEditor`에 설명 Text 한 단락을 넣어놨는데, [[설정 화면 설명 문구 전부 제거]] 피드백(제목+컨트롤만)과 어긋나고 이 개념은 사용자 본인이 제안한 것이라 삭제 — 다른 RuleCard와 동일하게 드롭다운만 남김. (단 `자동 퇴근 처리` 카드는 여전히 설명 Text 유지 중 — 설명이 전면 금지는 아니고 복잡한 규칙엔 남아있음. 마진은 단순+사용자 제안이라 뺀 것.)
- **검증**: `LeaveTimestampTest`(5케이스: 19:18→19:15, margin0 그대로, 출근 바닥 클램프, floor null, floor>lastSeen 포기) + 전체 testDebugUnitTest BUILD SUCCESSFUL. JDK는 `C:\Program Files\Android\openjdk\jdk-21.0.8` (JAVA_HOME 미설정이라 매번 세팅 필요; PATH엔 JRE8뿐이라 빌드 불가). adb도 PATH에 없음 → `C:\Program Files (x86)\Android\android-sdk\platform-tools\adb.exe`.
- **실기기 검증(재개 세션)**: assembleDebug+installDebug 크래시 없음, 서비스 정상 가동. 설정 화면에 "퇴근 시각 마진" 카드가 설명 없이 기본 3분으로 렌더 확인, 드롭다운 0~10분 정상, 5분 선택→저장 반영→3분(기본)으로 복원까지 uiautomator로 확인. 실제 퇴근 시각이 마진만큼 앞당겨 기록되는지는 물리적으로 회사 AP 범위를 벗어나야 재현 가능 → 산술은 유닛테스트로 갈음(기존 패턴). **아직 미커밋**.
