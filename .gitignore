*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/build

# Release signing (shared with TRide's key) — never commit these
/keystore.properties
app/*.jks
app/*.keystore
